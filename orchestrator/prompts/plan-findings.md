The previous plan was rejected with these findings — address every one:

{{#findings}}
- {{finding}}
{{/findings}}

Append a new `## Todos` section to `../ASSIGNMENT.md` that does, then submit
again. Number the list `1.` to `n.` consecutively.
