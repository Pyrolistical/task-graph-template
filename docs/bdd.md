# Behaviour

Every Given, When and Then in the suite, in the order they are written.
Generated by `bun run bdd` from `orchestrator/testing/bdd-doc.ts` — edit the
tests, not this file. How one is written is [Behaviour tests](bdd-tests.md).

889 behaviours across 38 suites.

## orchestrator/adapters/agents.test.ts

### Feature: loading the pool of agents

#### a slot is named for its type, provider, model and number

- **Given** a pool of two agents, one of them with three slots
- **When** the pool is loaded
- **Then** every slot has a name a person can read off the console

#### every slot of one entry shares that entry's agent key

- **Given** a pool with two slots of one agent and one of another
- **When** the pool is loaded
- **Then** the slots of one entry share a key, which is their name without its number

#### a slot of a model whose name carries dashes belongs to that model's agent

- **Given** the second slot of the model claude-sonnet-4-5, whose name carries dashes of its own
- **When** it is reduced to the agent it belongs to
- **Then** only the trailing slot number is dropped

#### a slot of a provider whose name carries a dot belongs to that provider's agent

- **Given** the first slot of the provider llama.cpp-rocm, whose name carries a dot
- **When** it is reduced to the agent it belongs to
- **Then** only the trailing slot number is dropped

#### an agent is enabled unless the pool says otherwise

- **Given** a pool with one agent left as the file found it
- **Given** the other agent in that pool turned off
- **When** the pool is loaded
- **Then** only that agent's slots start disabled

#### a slot may take every role unless the pool restricts it

- **Given** a pool with one agent that names no roles
- **Given** the other agent in that pool restricted to reviewing
- **When** the pool is loaded
- **Then** the first may work, review, plan and design, and the second may only review

#### a role the pipeline does not have is refused on load

- **Given** a pool naming a role no stage is run by
- **When** the pool is loaded
- **Then** it is refused at startup rather than at the first dispatch

#### a pool entry asking for retries is refused on load

- **Given** a pool entry asking for 3 retries, a setting the server does not read
- **When** the pool is loaded
- **Then** it is refused by name, so a setting that would do nothing is never believed

#### a pool entry asking for thinking is refused on load

- **Given** a pool entry asking for high thinking, a setting the server does not read
- **When** the pool is loaded
- **Then** it is refused by name, so a setting that would do nothing is never believed

#### a slot is a type, provider, model and number, and nothing more

- **Given** a pool with one plain agent in it
- **When** the pool is loaded
- **Then** the slot carries its name, its agent key, the type, provider and model
- **Then** the slot carries its number, whether it is enabled, its write paths and its roles

#### an agent writes to the toolchain cache unless told otherwise

- **Given** a pool that declares no write paths at all
- **When** the pool is loaded
- **Then** the write path defaults to the zig cache, which is the cache home

#### declared write paths replace the default rather than adding to it

- **Given** a pool declaring the two caches its toolchains need
- **When** the pool is loaded
- **Then** those are the only paths outside its worktree the agent may write

#### a pool declaring no write paths grants nothing but pi's home

- **Given** a pool that declares an empty list of write paths
- **When** the paths that agent may write are worked out
- **Then** only pi's own home comes along, because pi cannot run without it

#### a pi agent always gets its own home, declared or not

- **Given** a pi agent declaring one path of its own
- **When** the paths it may write are worked out
- **Then** pi's home comes on top of what it declared, because pi cannot run without it

#### an agent of another type gets only what it declared

- **Given** an agent that is not pi, declaring one path of its own
- **When** the paths it may write are worked out
- **Then** it is given that path and nothing besides

#### a check may write everything any agent in the pool declared

- **Given** a pool of two agents declaring overlapping write paths
- **When** the paths a check may write are worked out
- **Then** every declared path is there once, and no agent's home is

#### everything wrong with a pool file is reported at once

- **Given** a pool with a bad slot count in one entry and no type in another
- **When** the pool is loaded
- **Then** both are named by their place in the file, so one edit fixes it

#### the same model declared twice is refused

- **Given** a pool naming one model in two entries
- **When** the pool is loaded
- **Then** it is refused, because the two entries would share every slot name

#### a pool with no agents at all loads as no slots

- **Given** a pool file with an empty list of agents
- **When** the pool is loaded
- **Then** it holds no slots, so the manager can still author tasks with it

#### the pool a project is seeded with ships turned off

- **Given** the pool file a new project starts from
- **When** the pool file is loaded
- **Then** it has slots in it, and none of them will run until a person says so

### Feature: the sandbox an agent is spawned into

#### the whole filesystem is read-only before anything is opened

- **Given** the policy an agent is sandboxed under
- **When** the sandbox is built
- **Then** it starts by binding everything read-only, and ends at the command

#### the repo is re-exposed after the temporary directory is masked

- **Given** a policy whose repo could sit anywhere, including under the mask
- **When** the sandbox is built
- **Then** the repo is bound read-only again after the mask, so it survives

#### a writable path inside a read-only one still wins

- **Given** a policy whose worktree sits inside a read-only directory
- **When** the sandbox is built
- **Then** the writable bind comes last, so it is the one that takes effect

#### pi's home is a throwaway overlay, so its locks never persist

- **Given** a policy that overlays pi's home
- **When** the sandbox is built
- **Then** the home is mounted as a temporary overlay over the real one

#### the only namespace shared with the host is the network

- **Given** the policy an agent is sandboxed under
- **When** the sandbox is built
- **Then** everything but the network is unshared, because the model is remote

#### no editor can open inside the sandbox

- **Given** the policy an agent is sandboxed under
- **When** the sandbox is built
- **Then** every editor variable is set to something that exits at once

#### a commit with no message fails rather than waiting forever

- **Given** a repository with something staged, and the sandbox's environment
- **When** a bare git commit is run under it
- **Then** it fails at once instead of blocking on an editor that never opens

#### an agent is not killed when the manager exits

- **Given** the policy an agent is sandboxed under
- **When** the sandbox is built
- **Then** it is not tied to its parent, because agents outlive the manager

#### the command starts in the workspace it was given

- **Given** a policy naming the worktree the agent works in
- **When** the sandbox is built
- **Then** the command is run from that directory

#### a declared write path that does not exist is skipped

- **Given** a declared path nothing has created yet
- **When** the overlays are worked out on a host that can overlay
- **Then** it is skipped, because there is nothing there to overlay

#### a write path of the home shorthand alone becomes the home directory

- **Given** a pool entry declaring the write path as the tilde alone
- **When** the declared path is expanded
- **Then** it becomes the real home directory

#### a write path under the home shorthand keeps what follows it

- **Given** a pool entry declaring the write path as the cache below the tilde
- **When** the declared path is expanded
- **Then** it becomes the cache below the real home directory

#### a write path written in full is left as it stands

- **Given** a pool entry declaring the absolute write path of a directory
- **When** the declared path is expanded
- **Then** it is untouched, because there is no shorthand in it

#### a path declared twice is mounted once

- **Given** the same path declared by two agents in the pool
- **When** the overlays are worked out on a host that can overlay
- **Then** it is mounted once, because bwrap would refuse the second

#### a limited sandbox wraps the cgroup scope outermost

- **Given** a host that can create cgroup scopes
- **When** the sandbox is built
- **Then** the scope capping memory at 8G comes first, then the score of 300, then bwrap

#### a runaway sandbox is capped on memory, swap and processes

- **Given** the policy an agent is sandboxed under
- **When** the cgroup limits are worked out
- **Then** all three caps are set, so one agent cannot take the machine down

#### a crashed agent leaves no cgroup scope behind

- **Given** the policy an agent is sandboxed under
- **When** the cgroup limits are worked out
- **Then** the scope is collected when it dies, however it dies

#### checks are killed before agents, and both before the user's work

- **Given** the oom score adjustment a check and an agent are run with
- **When** they are lined up against the zero the user's own processes run at
- **Then** the check is 400, the agent 300 and the user's work 0, so the check dies first

#### the oom adjustment comes from the policy, not a constant

- **Given** a policy asking for its own oom score
- **When** the cgroup limits are worked out
- **Then** choom is told the policy's own 400, not the 300 an agent defaults to

#### a host with no cgroup support still gets a sandbox

- **Given** a host where creating a cgroup scope failed
- **When** the sandbox is built
- **Then** it degrades to bare bwrap rather than refusing to run at all

### Feature: the sandbox as it actually runs

#### the caps we ask for are the caps the kernel applies

- **Given** a check sandboxed with the limits a check is given
- **When** it reads its own cgroup and oom score from inside the sandbox
- **Then** it is capped at 8G of memory with no swap, at 512 tasks, and at the score 400

#### the repository can be read but never written

- **Given** a sandbox with the repository bound readable
- **When** a command reads from the repository and then writes to it
- **Then** the read succeeds, the write is refused, and nothing lands on disk

#### the workspace is writable and the toolchain is readable

- **Given** a sandbox with only its own workspace writable
- **When** a command writes in the workspace and reads the installed toolchain
- **Then** the file written in the workspace reads back, and the toolchain under /usr/local is there to read

#### a toolchain's cache is writable and dies with the sandbox

- **Given** a sandbox with the toolchain cache overlaid
- **When** a command writes into the cache
- **Then** it succeeds inside, and leaves nothing on the real cache afterwards

#### an agent commits in its worktree without touching the repo

- **Given** a worktree cloned from a repository the sandbox may only read
- **When** a command commits inside the worktree
- **Then** the commit lands in the worktree, and the repo has no branch for it yet

## orchestrator/adapters/check-runner.test.ts

### Feature: running a task's declared checks

#### a check that fails comes back with its code and both its streams

- **Given** a check that writes to stdout and stderr and then fails
- **When** the check is run against the task's worktree
- **Then** the exit code comes back, so the server knows the check failed
- **Then** the tail carries both streams, which is what the agent is shown
- **Then** the whole output is on disk, for a person to read afterwards

#### a check appears in the view while it runs and leaves it when it ends

- **Given** a check that takes long enough to be seen running
- **When** the check is started
- **Then** the console can see what is running and under which process
- **Then** the view empties again once the check is done

#### the output kept is the end of it, not the start

- **Given** a check that wrote far more output than an agent can be shown
- **When** the output is cut down to what the agent will read
- **Then** it is the last lines, which is where a failure is reported

## orchestrator/adapters/files.test.ts

### Feature: replacing a file without ever showing a half written one

#### a published view is never seen half written

- **Given** a view that has already been published once
- **When** it is published again
- **Then** the reader sees the new document whole, with no partial left behind

#### a failed publish leaves no poison for the next

- **Given** a path a publish can never land on, because a directory is in the way
- **When** a publish fails at it, the way is cleared, and the publish is tried again
- **Then** the same path still publishes whole, with no temp file left behind

#### concurrent publishes to one file both land whole

- **Given** a view that has already been published once
- **When** two publishes race to replace it
- **Then** both land, without tripping over one another's temp file

#### a reader beside a replacement sees one whole version

- **Given** a document long enough that one write is not one syscall
- **When** a hundred reads are taken while the document is replaced under them
- **Then** not one of them caught the file part way through

#### a path with nothing at it does not exist

- **Given** a directory with nothing written in it yet
- **When** the path is asked for before and after it is written
- **Then** only the second answer is yes

## orchestrator/adapters/git.test.ts

### Feature: running git subprocesses

#### a failing git command reports its stderr

- **Given** a git command that names a ref no repository has
- **When** the command is run
- **Then** it fails and the failure carries git's own words

#### gitOrThrow names the failure and its stderr

- **Given** a git command that fails
- **When** it is run through gitOrThrow
- **Then** the error says which exit and what git reported

## orchestrator/adapters/paced.test.ts

### Feature: a loop that runs one pass at a time

#### passes never overlap, however often a redraw is asked for

- **Given** a loop whose body takes a while
- **When** redraws are asked for while a pass is still running
- **Then** each pass finished before the next began

#### redraws asked for during a pass collapse into one more pass

- **Given** a loop that would otherwise sit idle for a long tick
- **When** two redraws are asked for during the first pass
- **Then** only one further pass ran, rather than one per request

#### a stopped loop does not wait out its tick

- **Given** a loop with a tick far longer than this test would wait for
- **When** the body stops the loop
- **Then** it returns right away

#### work notified on the shared latch runs the next pass at once

- **Given** a loop paced by a latch someone else notifies
- **When** work is notified after a pass has finished
- **Then** the next pass ran without waiting out the tick

#### a body that throws ends the loop and surfaces the failure

- **Given** a loop whose body fails
- **When** the body throws
- **Then** the failure is not swallowed, and no further pass runs

## orchestrator/adapters/pi-process.test.ts

### Feature: splitting the rpc stream into records

#### records are split on newlines and nothing else

- **Given** a record whose text carries a separator of its own
- **When** it is fed to the splitter
- **Then** it comes back whole, because only a newline ends a record

#### a record split across reads is held until it is whole

- **Given** the first half of a record, with no newline in it yet
- **When** the rest of it arrives
- **Then** the whole record comes back and nothing is left pending

#### a carriage return before the newline is dropped

- **Given** a record written with a carriage return before its newline
- **When** it is fed to the splitter
- **Then** the record is clean enough to parse

#### a line that is not a record is skipped

- **Given** a stream carrying a garbled line before a real record
- **When** it is fed to the stream
- **Then** the garbage is dropped and the record after it still lands

### Feature: knowing when an agent's turn is over

#### the end of a message is not the end of a turn

- **Given** a turn that has ended a message and will retry
- **When** the agent ends that message
- **Then** the turn is not settled, because pi may still retry inside it

#### the turn is over when pi says it has settled

- **Given** a turn that has started and ended a message
- **When** pi says the turn has settled
- **Then** the server may act on what the turn produced

#### the settle of one turn does not satisfy the next

- **Given** a turn that has already started and settled
- **When** a new turn is started and waited on
- **Then** only the new turn's own settle satisfies the wait

#### the outcome of a turn is its last assistant message

- **Given** an assistant message that ended in a provider error
- **When** it is fed to the stream
- **Then** the reason and its message are what the server will act on
- **Then** the cost is not tracked, because it says nothing about progress

#### a tool result is never mistaken for the outcome

- **Given** a tool result that failed
- **When** it is fed to the stream
- **Then** the turn has no outcome yet, because only the agent ends a turn

#### only an assistant message that spent tokens is measured

- **Given** a turn with a measured message, an unmeasured one and a tool result
- **When** all three are fed to the stream
- **Then** only the measured assistant message becomes a sample of the rate

### Feature: matching a reply to what was asked

#### a reply is matched to the request that carried its id

- **Given** a request waiting on a reply, with another reply arriving first
- **When** both of the replies arrive
- **Then** the waiting request is answered by its own reply

#### a dead process rejects what is already waiting

- **Given** a request waiting on a reply
- **When** the pi process dies under it
- **Then** the wait is rejected rather than left hanging

#### a dead process rejects what is asked of it afterwards

- **Given** a process that has already died
- **When** something else is asked of it
- **Then** it is rejected at once, naming what went wrong

#### a request made after the child died settles instead of hanging

- **Given** a pi process whose command exits immediately
- **When** something is asked of it
- **Then** it is rejected quickly, so a tick is never blocked on a dead agent

#### a process whose stdout closed is not alive, exit code or not

- **Given** a pi process whose command exits immediately
- **When** its stream settles for the last time
- **Then** the server reads it as gone, and will not prompt it again

#### an abort is recorded before the write that may fail

- **Given** a pi process whose command has already exited
- **When** it is aborted, and the write to the dead process fails
- **Then** the abort is still recorded, so the settle is read as an abort

### Feature: what an agent is doing right now

#### a running tool call is the activity, first line only

- **Given** an agent that has started a command spanning several lines
- **When** the tool call starts
- **Then** the activity names the tool and the first line of what it runs

#### a compaction is the activity while it runs

- **Given** an agent inside a tool call
- **When** it starts compacting instead
- **Then** the console shows the compaction and why it happened

#### every compaction is reported to the server, not just the first

- **Given** a stream watched for compactions
- **When** two compactions happen in one turn
- **Then** the server hears about both, so it can steer the agent each time

#### an agent between tool calls reads as thinking

- **Given** an agent inside a tool call
- **When** the tool call ends
- **Then** it reads as thinking, because the turn is not over

#### an agent that has settled is doing nothing

- **Given** an agent inside a tool call
- **When** the agent's turn settles
- **Then** the pane shows nothing, because the agent is waiting on the server

#### an agent that has just been prompted reads as thinking

- **Given** a stream about to carry a new turn
- **When** the turn is started
- **Then** the pane shows it thinking before its first tool call arrives

#### a tool call on a file is shown by the file it touches

- **Given** an agent reading a file
- **When** the tool call starts
- **Then** the path is what the console shows

#### a tool call with nothing to name falls back to its own name

- **Given** an agent calling a tool that takes no arguments worth showing
- **When** the tool call starts
- **Then** the tool's own name is what the console shows

#### a tool call with a target names both for the console

- **Given** an agent inside a bash call on a test run
- **When** the activity is written for the console
- **Then** the tool and its target are both named

#### a tool call with no target names only the tool

- **Given** an agent inside a bash call with nothing to point at
- **When** the activity is written for the console
- **Then** the tool alone is named, since the target was nothing

#### an agent that is thinking is shown thinking

- **Given** an agent thinking through its next step
- **When** the activity is written for the console
- **Then** the console reads that it is thinking

#### an agent that is compacting is shown doing so

- **Given** an agent compacting a context that overflowed
- **When** the activity is written for the console
- **Then** the console reads that it is compacting, and why

#### an idle agent shows no activity at all

- **Given** an agent with nothing going on
- **When** the activity is written for the console
- **Then** the line is empty, so the pane draws nothing for it

### Feature: noticing an agent that has stopped making progress

#### one command repeated to the limit is a loop

- **Given** a turn that has repeated one command one short of the limit
- **When** it runs the same command once more
- **Then** the server is told which command the agent is stuck on

#### a command that differs at all breaks the run

- **Given** a turn repeating one command, broken once by a different one
- **When** the original command is repeated again, up to one short of the limit
- **Then** it is not a loop, because the count starts over at any difference

#### the same command spread across turns is not a loop

- **Given** three turns, each repeating one command below the limit
- **When** all three turns run
- **Then** nothing is a loop, because a loop is repetition within one turn

#### a fresh prompt gives the agent a clean turn

- **Given** a turn the agent has looped in
- **When** the agent is prompted again
- **Then** the loop is forgotten, so the nudge is judged on its own turn

### Feature: how an agent is spawned

#### the spawn line puts pi in rpc mode with its extension

- **Given** the process a task is about to be dispatched to
- **When** the command line is built
- **Then** pi is put in rpc mode, approving tools, with the state's extension
- **Then** nothing is said in flags that the prompts say instead

#### a designer is prompted with words of its own

- **Given** the DESIGN state an agent is dispatched into
- **When** the fragment it is prompted with is read
- **Then** it is the shipped design prompt, and it says something

#### a design reviewer is prompted with words of its own

- **Given** the DESIGN_REVIEW state an agent is dispatched into
- **When** the fragment it is prompted with is read
- **Then** it is the shipped review prompt, and it says something

#### a planner is prompted with words of its own

- **Given** the PLAN state an agent is dispatched into
- **When** the fragment it is prompted with is read
- **Then** it is the shipped plan prompt, and it says something

#### a plan reviewer is prompted with words of its own

- **Given** the PLAN_REVIEW state an agent is dispatched into
- **When** the fragment it is prompted with is read
- **Then** it is the shipped review prompt, and it says something

#### a worker is prompted with words of its own

- **Given** the WORK state an agent is dispatched into
- **When** the fragment it is prompted with is read
- **Then** it is the shipped work prompt, and it says something

#### a work reviewer is prompted with words of its own

- **Given** the WORK_REVIEW state an agent is dispatched into
- **When** the fragment it is prompted with is read
- **Then** it is the shipped review prompt, and it says something

## orchestrator/adapters/prompt-files.test.ts

### Feature: overriding the words an agent reads

#### a project with no overrides reads every prompt as shipped

- **Given** a prompt store pointed at the orchestrator and nowhere else
- **When** a fragment is asked for
- **Then** it is the orchestrator's own copy of it

#### a project's own copy of a prompt wins

- **Given** a project that carries its own WORK prompt
- **When** the fragment is asked for
- **Then** the project's words are what the agent will read

#### a prompts path that is a regular file fails loudly

- **Given** a project whose prompts path is a file, not a directory
- **When** the prompts are loaded
- **Then** it fails, rather than silently reading no prompts at all

#### overriding one prompt leaves every other one alone

- **Given** a project that overrides only the WORK prompt
- **When** the other prompts are asked for
- **Then** each still comes from the orchestrator, rendered as it always was

#### an override is given the variables the shipped one is

- **Given** a project override that uses the variables of the file it replaces
- **When** it is rendered with a failed check
- **Then** the project's wording is filled in from the same variables

#### a prompt edited under a running server is not picked up

- **Given** a prompt store that has already cached an override
- **When** the file on disk is edited
- **Then** agents keep reading the cached copy, so a half-saved edit cannot leak

#### reloading picks up an edited override

- **Given** a prompt store whose cached override has since been edited
- **When** the store is reloaded
- **Then** the edit is cached and named among the files now in use

#### reloading after a deleted override falls back again

- **Given** a prompt store whose override has since been deleted
- **When** the store is reloaded
- **Then** the orchestrator's own copy is back in use, and no longer named

#### a prompt in neither directory names both of them

- **Given** a project with no overrides at all
- **When** a prompt that exists nowhere is asked for
- **Then** both places it looked are named, so the typo is easy to find

### Feature: filling a prompt in

#### a fragment asking for something it was not given fails

- **Given** a fragment that refers to a variable
- **When** it is rendered without that variable
- **Then** it fails and names what was missing, rather than rendering a blank

#### a section that is never closed fails

- **Given** a fragment with an unclosed section in it
- **When** the fragment is rendered
- **Then** it fails and names the section, rather than swallowing the rest

### Feature: the issues an agent is sent back for

#### an agent that says it is blocked is asked only once

- **Given** the issue raised when an agent reports it cannot continue
- **When** its budget of retries is read
- **Then** it gets a single attempt, because it is a claim about the world

#### an agent that left its todos missing is nudged four times

- **Given** the issue raised when an agent submits without a todo list
- **When** its budget of retries is read
- **Then** it is nudged four times before the task is held

#### an agent that left its notes missing is nudged four times

- **Given** the issue raised when an agent submits without notes
- **When** its budget of retries is read
- **Then** it is nudged four times before the task is held

#### an agent that modified its assignment is nudged four times

- **Given** the issue raised when an agent rewrites its own assignment
- **When** its budget of retries is read
- **Then** it is nudged four times before the task is held

#### an agent that did not commit is nudged four times

- **Given** the issue raised when an agent submits without committing
- **When** its budget of retries is read
- **Then** it is nudged four times before the task is held

#### an agent that changed the worktree is nudged four times

- **Given** the issue raised when an agent writes outside its branch
- **When** its budget of retries is read
- **Then** it is nudged four times before the task is held

#### a missing result is nudged the longest

- **Given** the issue raised when an agent settles without a result tool call
- **When** its budget is compared with the issues that mean a rule was broken
- **Then** it gets twice as many, being the cheapest thing an agent can fix

#### a loop is worth fewer nudges, being dearer to reach

- **Given** the issue raised when an agent repeats one command
- **When** its budget is compared with the cheapest and the dearest issues
- **Then** it sits between them, because each retry costs a whole turn

#### a held task says which rule the agent broke

- **Given** the issues raised when an agent submits without doing its part
- **When** each writes the reason the task will be held with
- **Then** each reads as something a person can act on without the transcript

#### a blocked task is held with the agent's own words

- **Given** an agent that reported what is standing in its way
- **When** the task is held for it
- **Then** the manager reads what the agent said, not a paraphrase of it

#### every issue has a prompt for every state that raises it

- **Given** every issue, named for each state an agent can be raised at in
- **When** each fragment it names is looked for on disk
- **Then** every one of them is there, so no issue can be raised wordlessly

### Feature: what an agent's result tool call means

#### a submit from WORK, which writes rather than reviews, carries nothing else

- **Given** WORK, the state an agent writes the work in
- **When** an agent in that state submits its work
- **Then** it is a plain submit, carrying no findings

#### a submit from PLAN, which writes rather than reviews, carries nothing else

- **Given** PLAN, the state an agent writes the plan in
- **When** an agent in that state submits its work
- **Then** it is a plain submit, carrying no findings

#### a submit from DESIGN, which writes rather than reviews, carries nothing else

- **Given** DESIGN, the state an agent writes the design in
- **When** an agent in that state submits its work
- **Then** it is a plain submit, carrying no findings

#### a design review submit carries the findings it was given

- **Given** a design reviewer that found one problem
- **When** it submits a review carrying that finding
- **Then** the finding comes back on the result

#### a plan review submit carries the findings it was given

- **Given** a plan reviewer that found one problem
- **When** it submits a review carrying that finding
- **Then** the finding comes back on the result

#### a work review submit carries the findings it was given

- **Given** a work reviewer that found one problem
- **When** it submits a review carrying that finding
- **Then** the finding comes back on the result

#### a designer that is stuck can report itself blocked

- **Given** a designer that cannot get past the thing in its way
- **When** it calls the blocked tool
- **Then** it reads as blocked, carrying what the agent said

#### a design reviewer that is stuck can report itself blocked

- **Given** a design reviewer that cannot get past the thing in its way
- **When** it calls the blocked tool
- **Then** it reads as blocked, carrying what the agent said

#### a planner that is stuck can report itself blocked

- **Given** a planner that cannot get past the thing in its way
- **When** it calls the blocked tool
- **Then** it reads as blocked, carrying what the agent said

#### a plan reviewer that is stuck can report itself blocked

- **Given** a plan reviewer that cannot get past the thing in its way
- **When** it calls the blocked tool
- **Then** it reads as blocked, carrying what the agent said

#### a worker that is stuck can report itself blocked

- **Given** a worker that cannot get past the thing in its way
- **When** it calls the blocked tool
- **Then** it reads as blocked, carrying what the agent said

#### a work reviewer that is stuck can report itself blocked

- **Given** a work reviewer that cannot get past the thing in its way
- **When** it calls the blocked tool
- **Then** it reads as blocked, carrying what the agent said

#### a designer is loaded with the extension its stage names

- **Given** the DESIGN stage of an agent
- **When** the extension file it names is looked for on disk
- **Then** it is the designer extension, and it is there to load

#### a design reviewer is loaded with the extension its stage names

- **Given** the DESIGN_REVIEW stage of an agent
- **When** the extension file it names is looked for on disk
- **Then** it is the review extension, and it is there to load

#### a planner is loaded with the extension its stage names

- **Given** the PLAN stage of an agent
- **When** the extension file it names is looked for on disk
- **Then** it is the planner extension, and it is there to load

#### a plan reviewer is loaded with the extension its stage names

- **Given** the PLAN_REVIEW stage of an agent
- **When** the extension file it names is looked for on disk
- **Then** it is the review extension, and it is there to load

#### a worker is loaded with the extension its stage names

- **Given** the WORK stage of an agent
- **When** the extension file it names is looked for on disk
- **Then** it is the worker extension, and it is there to load

#### a work reviewer is loaded with the extension its stage names

- **Given** the WORK_REVIEW stage of an agent
- **When** the extension file it names is looked for on disk
- **Then** it is the review extension, and it is there to load

#### a designer's submit tool carries nothing beyond the submit

- **Given** a designer, whose stage produces no findings
- **When** the extension it is spawned with is loaded and its submit read
- **Then** it offers both result tools, and its submit carries no fields

#### a design reviewer's submit tool carries findings

- **Given** a design reviewer, whose stage reviews the design
- **When** the extension it is spawned with is loaded and its submit read
- **Then** it offers both result tools, and its submit carries findings

#### a planner's submit tool carries nothing beyond the submit

- **Given** a planner, whose stage produces no findings
- **When** the extension it is spawned with is loaded and its submit read
- **Then** it offers both result tools, and its submit carries no fields

#### a plan reviewer's submit tool carries findings

- **Given** a plan reviewer, whose stage reviews the plan
- **When** the extension it is spawned with is loaded and its submit read
- **Then** it offers both result tools, and its submit carries findings

#### a worker's submit tool carries nothing beyond the submit

- **Given** a worker, whose stage produces no findings
- **When** the extension it is spawned with is loaded and its submit read
- **Then** it offers both result tools, and its submit carries no fields

#### a work reviewer's submit tool carries findings

- **Given** a work reviewer, whose stage reviews the work
- **When** the extension it is spawned with is loaded and its submit read
- **Then** it offers both result tools, and its submit carries findings

#### a designer's result tools end the turn they are called in

- **Given** the result tools a designer is spawned with
- **When** each of them is called with arguments its stage accepts
- **Then** neither leaves the agent a turn to carry on in

#### a design reviewer's result tools end the turn they are called in

- **Given** the result tools a design reviewer is spawned with
- **When** each of them is called with arguments its stage accepts
- **Then** neither leaves the agent a turn to carry on in

#### a planner's result tools end the turn they are called in

- **Given** the result tools a planner is spawned with
- **When** each of them is called with arguments its stage accepts
- **Then** neither leaves the agent a turn to carry on in

#### a plan reviewer's result tools end the turn they are called in

- **Given** the result tools a plan reviewer is spawned with
- **When** each of them is called with arguments its stage accepts
- **Then** neither leaves the agent a turn to carry on in

#### a worker's result tools end the turn they are called in

- **Given** the result tools an agent is spawned with
- **When** each of them is called with arguments its stage accepts
- **Then** neither leaves the agent a turn to carry on in

#### a work reviewer's result tools end the turn they are called in

- **Given** the result tools a work reviewer is spawned with
- **When** each of them is called with arguments its stage accepts
- **Then** neither leaves the agent a turn to carry on in

#### a reviewer is told how many findings it filed

- **Given** a work reviewer that found two defects
- **When** it submits both of them
- **Then** it reads back that the work was rejected, and by how much

#### a reviewer that files nothing accepts the work

- **Given** a work reviewer that could fault nothing
- **When** it submits an empty list of findings
- **Then** it reads back that it has accepted the work

#### an agent that stops on a wall is told who reads it

- **Given** a worker that cannot get past the thing in its way
- **When** it reports itself blocked
- **Then** it reads back that a person has the turn now

## orchestrator/adapters/runtime.test.ts

### Feature: where a project's runtime state lives

#### a project is keyed by the path it was opened at

- **Given** the absolute path of a checkout
- **When** the runtime key for it is worked out
- **Then** it is the path itself, flattened into one directory name

#### two clones of one project never share a runtime

- **Given** the same project checked out at a different path
- **When** the runtime key for it is worked out
- **Then** it is the clone's own flattened path, never the other checkout's

#### every path a task needs hangs off its own directory

- **Given** a runtime directory for a project
- **When** the paths of one task are worked out
- **Then** all of them sit under one directory, which discarding removes whole

#### the assignment sits beside the worktree, never inside it

- **Given** a runtime directory for a project
- **When** the assignment and the worktree of one task are compared
- **Then** the assignment is a sibling, so it never lands in a commit

#### preparing a task makes the directories it will write to

- **Given** a runtime directory a task has never been dispatched in
- **When** the task is prepared
- **Then** the history and both session directories are there to be written to

### Feature: where a project's task graph lives

#### a project directly under home is keyed by its own name

- **Given** the home directory of the user
- **Given** a project sitting directly below it, named project
- **When** the graph key of it is worked out
- **Then** the key is its name below home

#### a project nested under home is keyed by the names it sits below

- **Given** the home directory of the user
- **Given** a project two directories below it, at a then b
- **When** the graph key of it is worked out
- **Then** the key is its path below home, flattened into one name

#### the home directory itself is keyed by its whole path

- **Given** the home directory of the user
- **Given** the home directory is the project being worked in
- **When** the graph key of it is worked out
- **Then** the key is its whole path, flattened into one name

#### a project outside home is keyed by its whole path

- **Given** the home directory of the user
- **Given** a project in the temporary directory, outside home altogether
- **When** the graph key of it is worked out
- **Then** the key is its whole path, flattened into one name

#### the root the graph lives under can be pointed elsewhere

- **Given** an environment naming another root for task graphs
- **When** the task directory of a project is worked out
- **Then** it is that root joined with the project's key

#### the suite never writes a task graph into the real home

- **Given** the test rig, which points the root at a temporary directory
- **When** the task directory of a project is worked out
- **Then** it lands under the test root, and never in the user's own graph

### Feature: keeping a second server out of the runtime directory

#### the first server to start takes the directory

- **Given** a runtime directory no server has started against
- **When** the server takes the lock
- **Then** the lock names the server, so others can see who holds it

#### a server holding the directory keeps another out

- **Given** a runtime directory a live server already holds
- **When** another server tries to take the lock
- **Then** it refuses, naming the server that holds the directory

#### a stale lock from a dead server is taken over

- **Given** a runtime directory whose last server died without clearing its lock
- **When** a new server takes the lock
- **Then** the lock names the new server

#### two servers racing for a stale lock end with one holder

- **Given** a runtime directory with a stale lock, and two servers claiming it
- **When** both servers take the lock at once
- **Then** at least one of them holds it, and no one leaves it empty

#### a server clears the lock it holds

- **Given** a runtime directory the server holds
- **When** the server is done with it
- **Then** nothing is left to keep another server out

#### a server does not clear a lock it does not hold

- **Given** a lock some other server holds
- **When** the server clears the lock
- **Then** the other server's lock stays, still naming its holder

### Feature: the server log

#### the log keeps its most recent lines and stays bounded

- **Given** a server log with a small cap on how much it keeps
- **When** far more is written to it than it can hold
- **Then** what is left is under the cap, ends at the newest line, and is whole

#### concurrent log calls keep the bounded end of the log

- **Given** a server log with a small cap on how much it keeps
- **When** many lines are written to it all at once
- **Then** the log is bounded, ends at the newest line, and is whole

#### a log that was closed writes again if asked

- **Given** a server log that has been written to and then closed
- **When** something still in flight logs afterwards
- **Then** the line lands beside the earlier one instead of failing

### Feature: discarding a task that is finished with

#### the whole task directory goes with it

- **Given** a task that has been prepared and written to
- **When** the task is discarded
- **Then** nothing of it is left behind on disk

#### discarding a task that was never started is harmless

- **Given** a task that was closed before it was ever dispatched
- **When** the task is discarded
- **Then** nothing fails, because there was nothing to remove

### Feature: telling whether a process is still running

#### a process that is running reads as alive

- **Given** the pid of this process, which is certainly running
- **When** the candidate is checked
- **Then** it reads as alive

#### a pid nothing has ever had reads as dead

- **Given** a pid beyond anything this machine has handed out
- **When** the candidate is checked
- **Then** it reads as dead, so the reaper is free to take the task back

#### a child that exited is dead, waited on or not

- **Given** a detached child that has run to completion
- **When** its pid is checked
- **Then** it reads as dead, so a zombie never shields a task from the reaper

### Feature: the log of every transition applied

#### the sequence advances by one for each transition

- **Given** a fresh transition log
- **When** two transitions are appended
- **Then** each takes the next number, and the cursor reaches the last of them

#### a log reopened by a new server continues its sequence

- **Given** a log a previous server appended to
- **Given** a new server opening the same log
- **When** it appends to it
- **Then** it picks up where the last one left off, so no number repeats

#### a reader is given every transition in the order applied

- **Given** a log with three transitions in it
- **When** the log is read back
- **Then** every entry is there, numbered so a reader can find its own place

#### a log nothing has been written to reads as empty

- **Given** a log file that does not exist yet
- **When** the log is read
- **Then** it reads as empty rather than failing the first tick

#### a log that cannot be read fails loudly rather than reading empty

- **Given** a log path that is a directory, not a file
- **When** the log is opened
- **Then** it fails, rather than pretending no transitions ever happened

#### the file is trimmed but the sequence keeps counting

- **Given** a log that keeps only its last ten entries
- **When** twenty-five transitions are appended
- **Then** only the last ten are kept, still numbered from where they happened
- **Then** a server reopening the log carries on from the same number

#### concurrent appends all land, and none is lost to another's rewrite

- **Given** a log that keeps only its last ten entries
- **When** fifty transitions are appended all at once
- **Then** every one is acknowledged, and the newest ten are on disk

#### a reader never sees the log torn by an append or a trim

- **Given** a log that keeps only its last ten entries
- **When** reads and appends race each other, past the point where it trims
- **Then** every read is a whole log: complete entries, none torn by a rewrite

#### a transition still in flight at shutdown is recorded, not lost

- **Given** a transition log the manager has already closed on its way out
- **When** a settling agent appends one last transition
- **Then** it is written and numbered in turn, rather than failing the settle

### Feature: keeping what an agent wrote on an earlier attempt

#### the first dispatch of a task has nothing behind it

- **Given** a task that has never been dispatched
- **When** the attempt about to be made is counted
- **Then** it is the first

#### rotating never writes over what an agent wrote before

- **Given** a task whose first attempt has already been rotated into history
- **Given** a second attempt written to the live assignment
- **When** the live file is rotated into history
- **Then** both attempts are kept, numbered in the order they were made

#### rotating when there is nothing to rotate is harmless

- **Given** a task with no live assignment on disk
- **When** the assignment is rotated
- **Then** nothing is moved, and the dispatch carries on

### Feature: messages waiting for an agent's next turn

#### a second message is kept beside the first

- **Given** a task with one message already waiting
- **When** another message is queued for the same turn
- **Then** the agent is handed both of them, in one drain

#### a drain leaves nothing for the next turn

- **Given** a task whose waiting message has been drained
- **When** the next turn drains again
- **Then** it is handed nothing, rather than the message twice

### Feature: the workspace an agent works in

#### a workspace borrows the repository's objects

- **Given** a repository with a commit in it
- **Given** a path the clone will land on
- **When** a workspace is cloned from it
- **Then** it points at the repository's objects instead of copying them

#### a workspace inherits the repository's commit identity

- **Given** a repository configured with an author
- **Given** a path the clone will land on
- **When** a workspace is cloned from it
- **Then** the agent's commits carry that author, which a plain clone would not

#### a workspace starts on its task branch, cut from the base

- **Given** a repository with a base branch
- **Given** a path the clone will land on
- **When** a workspace is cloned for a task
- **Then** it is on the task's own branch, standing where the base does

#### a recloned workspace comes back to the work already done

- **Given** a workspace whose commits were harvested before it was removed
- **Given** a path the new clone will land on
- **When** it is cloned again for the same task
- **Then** it checks out the branch that survived, not the base it was cut from

#### harvesting lands the branch where the manager can see it

- **Given** a workspace with a commit the repository has never seen
- **When** the workspace is harvested
- **Then** the branch is in the repository, and the base is untouched

#### harvesting a linked worktree does nothing rather than fail

- **Given** a linked worktree, which already shares the repository's refs
- **When** the worktree is harvested
- **Then** the branch is already where it needs to be, and the fetch is skipped

#### a cloned workspace has refs of its own

- **Given** a repository with a commit in it
- **Given** a path the clone will land on
- **When** a workspace is cloned from it
- **Then** it does not share the repository's refs, so harvesting is a real fetch

#### harvesting after a rebase replaces the branch that was there

- **Given** a harvested branch whose base has since moved on, and a rebase onto it
- **When** the workspace is harvested again
- **Then** the stale branch is replaced by the rebased one, ready to merge

#### syncing brings the workspace's copy of the base forward

- **Given** a workspace whose base has moved on in the repository
- **When** the base is synced into the workspace
- **Then** the workspace can rebase onto what the base has actually become

#### a fresh workspace carries no commit and no change

- **Given** a repository with a commit in it
- **Given** a path the clone will land on
- **When** a workspace is cloned from it
- **Then** the guard sees nothing committed and nothing changed

#### a commit in the workspace is counted against the base

- **Given** a workspace with nothing committed in it
- **When** the agent commits in it
- **Then** the guard sees the commit it made

#### an untracked or modified file counts as uncommitted work

- **Given** a workspace with one file added and one changed
- **When** the workspace is read for uncommitted work
- **Then** both are reported, so a submit without a commit can be caught

#### staged work is still uncommitted work

- **Given** a workspace whose changes have been staged but not committed
- **When** the workspace is read for uncommitted work
- **Then** staging does not count as committing

#### a long list of changes is cut down for a prompt

- **Given** a workspace with far more changed files than a prompt should carry
- **When** the list is written for the prompt the agent will read
- **Then** it is capped, and says how many more there were

#### a short list of changes is written out in full

- **Given** a workspace with three changed files
- **When** the list is written for the prompt the agent will read
- **Then** all of them are named, because they fit

#### the base a recloned workspace measures against is its remote

- **Given** a workspace recloned from a branch that had already been harvested
- **When** its commits are counted against the base
- **Then** the count is right even though it has no local branch by that name

#### the commit count stays the agent's own as the base moves

- **Given** a workspace with one commit, and a base that has moved on since
- **When** its commits are counted against the base
- **Then** only the agent's own commit is counted, not the base's

#### removing a workspace leaves the repository untouched

- **Given** a workspace cloned from a repository
- **When** the workspace is removed, and removed again
- **Then** it is gone, the repository is clean, and removing it twice is harmless

## orchestrator/adapters/task.test.ts

### Feature: the id a task is known by

#### the first task of a project is written as the id 000001

- **Given** the number one, the first task a project holds
- **When** the number is written as an id
- **Then** it is padded to six digits, so ids sort as they were created

#### the forty-second task is written as the id 000042

- **Given** the number forty-two, a task part way into a project
- **When** the number is written as an id
- **Then** it is padded to six digits, so ids sort as they were created

#### the last task six digits can hold keeps its own digits

- **Given** the number nine hundred and ninety-nine thousand, nine hundred and ninety-nine
- **When** the number is written as an id
- **Then** it is written as it stands, because it already fills six digits

#### a padded six digit string is a task id

- **Given** the string 000001, as the first task of a project is written
- **When** the candidate is checked
- **Then** it is a task id

#### a six digit string with no padding is a task id

- **Given** the string 999999, as the last task six digits can hold is written
- **When** the candidate is checked
- **Then** it is a task id

#### a single digit is not a task id

- **Given** the string 1, as a person writing an id by hand might shorten it
- **When** the candidate is checked
- **Then** it is refused as an id, because an id is six digits and nothing else

#### a string of five digits is not a task id

- **Given** the string 00001, one digit short of an id
- **When** the candidate is checked
- **Then** it is refused as an id, because an id is six digits and nothing else

#### a string of seven digits is not a task id

- **Given** the string 0000001, one digit past an id
- **When** the candidate is checked
- **Then** it is refused as an id, because an id is six digits and nothing else

#### a string carrying letters is not a task id

- **Given** the string abc123, as a hand-edited document might carry in place of an id
- **When** the candidate is checked
- **Then** it is refused as an id, because an id is six digits and nothing else

#### the number YAML reads from an unquoted id is not an id

- **Given** the number forty-two, as YAML reads an id nobody quoted
- **When** the candidate is checked
- **Then** it is not an id, because an id is a quoted string

#### the null YAML reads from a missing id is not an id

- **Given** no value at all, as a document without an id leaves it
- **When** the candidate is checked
- **Then** it is not an id, because an id is a quoted string

### Feature: splitting a task document

#### a body full of rules is kept exactly as it was written

- **Given** a document whose body carries its own horizontal rules
- **When** the document is split into frontmatter and body
- **Then** only the first rule ends the frontmatter, and the body is untouched

#### a document with no frontmatter at all is refused

- **Given** a file that is prose and nothing else
- **When** the document is split
- **Then** it is refused, rather than read as a task with no fields

#### the shipped template carries every field the schema has

- **Given** the template a new task is copied from
- **When** its frontmatter is read
- **Then** every field is there, in the state a new task starts in

#### the shipped template leaves only the id and title to fill in

- **Given** the template a new task is copied from
- **When** it is parsed as though it were a task
- **Then** the only two fields it fails on are the two a person supplies

### Feature: reading and writing a task's fields

#### a document written and read back is unchanged

- **Given** a task using every field, with awkward characters in its title
- **Given** the document written once
- **When** it is parsed and written again
- **Then** nothing drifts, so a person's edit and the server's agree

#### ids are written quoted, so their leading zeros survive

- **Given** a task with a dependency, both with leading zeros in their ids
- **Given** the document written out
- **When** it is read back
- **Then** the ids are quoted on disk and come back as strings

#### an id someone unquoted by hand is refused

- **Given** a document whose id YAML will read as a number
- **When** it is parsed as a task
- **Then** it is refused, rather than silently becoming task forty-two

#### a title carrying a colon survives being written and read

- **Given** the title Add: colon, whose colon YAML reads as a key
- **When** it is written into a document and read back
- **Then** it comes back as the person typed it

#### a title carrying a hash survives being written and read

- **Given** the title fix #hash, whose hash YAML reads as a comment
- **When** it is written into a document and read back
- **Then** it comes back as the person typed it

#### a title carrying a quote and a backslash survives being written and read

- **Given** a title carrying both the quote and the backslash a quoted field escapes
- **When** it is written into a document and read back
- **Then** it comes back as the person typed it

#### a title opening with a dash survives being written and read

- **Given** the title - dash, whose leading dash YAML reads as a list entry
- **When** it is written into a document and read back
- **Then** it comes back as the person typed it

#### a title of digits survives being written and read

- **Given** the title 123, which YAML would otherwise read as a number
- **When** it is written into a document and read back
- **Then** it comes back as the person typed it, still a string

#### a title YAML would read as nothing survives being written and read

- **Given** a title YAML would otherwise read as nothing
- **When** it is written into a document and read back
- **Then** it comes back as the person typed it, still a string

#### a hold reason spanning many lines survives

- **Given** the reason a review failure writes, one bulleted line per finding
- **When** it is written into a document and read back
- **Then** the newlines stay inside the one quoted field, and come back whole

#### a field that is missing and one that is unknown both report

- **Given** a document with a stray field and most of the real ones missing
- **When** it is parsed as a task
- **Then** the stray field and the missing ones are all named at once

#### every violation is reported at once, not just the first

- **Given** a document with four separate things wrong with it
- **When** it is parsed as a task
- **Then** all of them come back, so one edit can fix the document

#### a task claimed by an agent with no process is refused

- **Given** a claim naming the agent a but carrying no process
- **When** it is parsed as a task
- **Then** it is refused, because a claim without a process cannot be reaped

#### a task holding a process but no agent is refused

- **Given** a claim carrying the process 12 but naming no agent
- **When** it is parsed as a task
- **Then** it is refused, because a process nobody owns cannot be reaped

#### a check with an empty command line is refused

- **Given** a task whose first check is an empty string
- **When** it is parsed as a task
- **Then** it is refused as too small, naming the entry that is wrong

#### a check written as a mapping rather than a command line is refused

- **Given** a task whose first check is a mapping of a command
- **When** it is parsed as a task
- **Then** it is refused as not a string, naming the entry that is wrong

#### an empty list is written as such, and reads back empty

- **Given** a task depending on nothing and declaring no checks
- **When** it is written and read back
- **Then** the lists are written inline, and come back as empty lists

#### fields are always written in the same order

- **Given** a task with every field on it
- **When** the document is written
- **Then** the fields keep the schema's order, so a diff shows only real changes

### Feature: finding a dependency that can never be satisfied

#### a graph that only flows one way has no cycle

- **Given** tasks depending only on tasks created before them
- **When** the graph is searched for cycles
- **Then** nothing is reported, because every task can eventually run

#### two tasks waiting on each other are both reported

- **Given** two tasks each depending on the other
- **When** the graph is searched for cycles
- **Then** both are named, because neither can ever unblock

#### a cycle through a third task is found too

- **Given** three tasks depending on one another in a ring
- **When** the graph is searched for cycles
- **Then** all three are named, however long the ring is

#### a dependency on a task that does not exist is not a cycle

- **Given** a task depending on an id no document carries
- **When** the graph is searched for cycles
- **Then** nothing is reported, because the dependency is a typo, not a ring

### Feature: creating a task

#### a new task is written with a quoted id and a fresh state

- **Given** an empty task directory
- **When** a task is created in it
- **Then** it is the first id, quoted on disk and read back as a string
- **Then** it starts as new, with the moment it entered that state

#### a title carrying replacement patterns is stored verbatim

- **Given** an empty task directory
- **Given** the title Fix $& and $` and $' handling, whose patterns a replacement would expand
- **When** a task is created with it
- **Then** it comes back exactly as it was typed

#### a title carrying YAML punctuation is stored verbatim

- **Given** an empty task directory
- **Given** the title Add: colon, #hash and a "quote", whose punctuation YAML gives meaning to
- **When** a task is created with it
- **Then** it comes back exactly as it was typed

#### a task with an empty title is refused

- **Given** an empty task directory
- **Given** a title with no characters in it at all
- **When** a task is created with it
- **Then** it is refused, because a task nobody named cannot be worked on

#### a task titled with only spaces is refused

- **Given** an empty task directory
- **Given** a title of three spaces, with nothing in it a person could read
- **When** a task is created with it
- **Then** it is refused, because a task nobody named cannot be worked on

#### the first task created takes the id 000001

- **Given** an empty task directory
- **When** the first task is created
- **Then** it takes the id 000001, and the counter reads two for the next one

#### the second task created takes the id 000002

- **Given** a task directory holding the first task
- **When** the second task is created
- **Then** it takes the id 000002, and the counter reads three for the next one

#### the third task created takes the id 000003

- **Given** a task directory holding the first two tasks
- **When** the third task is created
- **Then** it takes the id 000003, and the counter reads four for the next one

#### a new task's body is the template's, unchanged

- **Given** an empty task directory
- **When** a task is created
- **Then** its body is what the template carries, for a person to fill in

#### a project's own template is used over the shipped one

- **Given** a task directory carrying its own template
- **When** a task is created
- **Then** the project's sections are what a new task starts with

#### a counter someone has corrupted fails loudly

- **Given** a task directory whose counter is not a number
- **When** a task is created
- **Then** it fails and names the file, rather than guessing an id

#### a task file that already exists is never written over

- **Given** a task directory whose counter has been wound back over a task
- **When** a task is created at the id that is already taken
- **Then** it fails, and the task already there is untouched

#### a create that fails leaves the graph open to the next one

- **Given** a task directory whose counter has been wound back over a task
- **When** a create collides with the task and fails
- **Then** a fresh create still takes the id the counter owes

#### a graph another server holds is refused, not waited for

- **Given** a task directory a live server has taken for itself
- **When** a second server tries to take the same graph
- **Then** it refuses, naming the server that holds the graph

#### a graph whose server died without clearing is taken over

- **Given** a task directory whose last server died holding it
- **When** a new server takes the graph
- **Then** the lock names the new server, so a crash never wedges the graph

### Feature: rewriting the body of a task

#### the body is replaced and the fields are left alone

- **Given** a task an agent is working on
- **When** its body is rewritten
- **Then** the new body is on disk and none of the fields moved

#### a body is normalized to the spacing every document has

- **Given** a body with stray blank lines above and below it
- **When** that body is written to the task
- **Then** it is stored with one blank line above and one newline below

#### an empty body is refused and the document is untouched

- **Given** a task and a body that is only whitespace
- **When** that body is written
- **Then** it is refused, and the document is left exactly as it was

#### a body written to a task that does not exist is refused

- **Given** an empty task directory
- **When** a body is written to an id nothing carries
- **Then** it is refused, naming the task that was not found

#### a body written before a transition survives it

- **Given** a task whose body the manager has just rewritten
- **When** the task is held straight afterwards
- **Then** both the prose and the hold are on disk, because both took the lock

## orchestrator/adapters/transitions.test.ts

### Feature: a task that waits on other tasks

#### a task with no dependencies starts as soon as it is submitted

- **Given** a new task nothing was edited into
- **When** the task is submitted
- **Then** it enters the design phase straight away

#### a task with a dependency waits instead of starting

- **Given** a new task edited to depend on another
- **When** the task is submitted
- **Then** it waits, still carrying the dependency it is waiting on

#### a blocked task submitted again stays where it is

- **Given** a task already blocked behind a dependency
- **When** it is submitted again
- **Then** the machine moves it nowhere, and it keeps waiting

#### a blocked task starts once its last dependency is gone

- **Given** a task blocked behind two dependencies, one since edited out
- **Given** the last dependency edited out of it
- **When** it is submitted with its dependencies gone
- **Then** it starts, because nothing is left for it to wait on

#### a dependency on a task that is gone can be edited away

- **Given** a task blocked on an id no document carries any more
- **When** the task is submitted
- **Then** it starts, rather than waiting on something that will never close

### Feature: taking and clearing a claim

#### a claim records the agent and its process, not a new state

- **Given** a task waiting in the work stage
- **When** an agent claims it
- **Then** the holder and its process are recorded, and the stage is unchanged

#### a task an agent already holds cannot be claimed again

- **Given** a task an agent is working on
- **When** a second agent claims it
- **Then** it is refused, and the first agent keeps it

#### a claim whose process is still alive is not cleared

- **Given** a task claimed by an agent that is still running
- **When** the claim is cleared
- **Then** it is refused, so a live agent is never reaped

#### a claim whose process is gone is cleared where it stands

- **Given** a task claimed by an agent whose process has exited
- **When** the claim is cleared
- **Then** the task keeps its stage and goes back into the queue unheld

#### a state no agent runs has no claim to clear

- **Given** a task in a state the server drives rather than an agent
- **When** a claim is cleared on it
- **Then** it is refused, because there was never a holder to release

#### a state no agent runs cannot be claimed

- **Given** a task in a state the server drives rather than an agent
- **When** an agent claims it
- **Then** it is refused, and nothing is written onto the task

#### a claim by an agent named only with spaces is refused

- **Given** a task ready to be claimed
- **When** a claim is made under a name of two spaces, with the process 12
- **Then** it is refused for the empty name, and the task is left unclaimed in DESIGN

#### a claim carrying the process zero is refused

- **Given** a task ready to be claimed
- **When** the agent a claims it with the process 0
- **Then** it is refused for the process, and the task is left unclaimed in DESIGN

#### a claim carrying a fractional process is refused

- **Given** a task ready to be claimed
- **When** the agent a claims it with the process 1.5
- **Then** it is refused for the process, and the task is left unclaimed in DESIGN

### Feature: what a review writes into the task body

#### a work review's finding is appended for the worker to read

- **Given** finished work under review
- **When** the reviewer sends it back with a finding
- **Then** the worker gets the task back with the finding written into it

#### every finding in a review lands in the body

- **Given** finished work under review
- **When** the reviewer sends it back with two findings
- **Then** both are written in, so none is lost between reviews

#### the manager's findings are appended the same way

- **Given** a task waiting on the manager
- **When** the manager sends it back with a finding
- **Then** the worker reads it exactly as it reads an agent reviewer's

#### a second round of findings is appended below the first

- **Given** work sent back once already and submitted again
- **When** the reviewer sends it back a second time
- **Then** both rounds are in the body, so the history of the task is one file

#### a plan review's findings never touch the body

- **Given** a plan under review
- **When** the reviewer sends it back
- **Then** the planner rewrites the body itself, so nothing is appended

#### a review with no findings at all is refused

- **Given** finished work under review
- **When** the reviewer sends it back with an empty list
- **Then** it is refused, and the document is untouched

#### an accepted plan becomes the task's body

- **Given** a plan under review
- **When** the reviewer accepts it
- **Then** what was accepted is what the task carries from here on

#### a worker's notes become the task's body

- **Given** a task an agent has finished working on
- **When** the worker submits the assignment it wrote
- **Then** the task carries the notes into its checks

#### a task in WORK cannot submit without a body

- **Given** a task an agent is working on, which hands in the work it wrote
- **When** it submits with no body
- **Then** it is refused for the missing body

#### a task in PLAN_REVIEW cannot submit without a body

- **Given** a task under plan review, which hands in the plan it accepted
- **When** it submits with no body
- **Then** it is refused for the missing body

#### a task in DESIGN_REVIEW cannot submit without a body

- **Given** a task under design review, which hands in the design it accepted
- **When** it submits with no body
- **Then** it is refused for the missing body

#### findings written into the body survive to the closed file

- **Given** work that was sent back with a finding and then finished
- **When** the manager closes the task
- **Then** the closed document still carries why the work was sent back

### Feature: the checks a task carries

#### checks edited in before a task starts survive into design

- **Given** a new task a person has written checks onto
- **When** the task is submitted
- **Then** it starts with the checks it will be held to

#### a check is a command and never a record of having passed

- **Given** a task in its checks, with another check edited in
- **When** the task's checks pass
- **Then** the checks are still just commands, with no result written beside them

#### a failed check sends the task back to the worker

- **Given** a task whose checks are running
- **When** the checks run and fail
- **Then** the task goes back to work

#### a failed check writes nothing into the graph

- **Given** a task whose checks are running
- **When** the checks run and fail
- **Then** only the state moved, because the failure is told to the agent

#### a reviewer cannot fail the task it is reviewing

- **Given** finished work under review
- **When** the reviewer tries to fail it as a check would
- **Then** it is refused, because a review sends findings back instead

### Feature: closing a task

#### a task the manager accepts is closed and moved aside

- **Given** a task waiting on the manager
- **When** the manager accepts it
- **Then** the document leaves the active graph for the closed directory

#### a task the manager rejects outright is closed too

- **Given** a task waiting on the manager
- **When** the manager aborts it
- **Then** it closes, because aborting is throwing the work away

#### a task an agent is still holding cannot be aborted

- **Given** a task in the design phase
- **When** the manager aborts it
- **Then** it is refused, because a task is held before it is thrown away

#### a task held out of design can then be aborted

- **Given** a task in the design phase, held by the manager
- **When** the manager aborts it
- **Then** it closes without ever having been worked on

#### a task sent back by a failed check can be abandoned

- **Given** a task returned to work by a failed check, and then held
- **When** the manager aborts it
- **Then** it closes, and the work in progress is thrown away with it

#### closing a task frees everything that waited on it

- **Given** a task blocked behind a dependency
- **When** the dependency is closed
- **Then** the dependency is edited out of it and it starts

#### a task still waiting on something else stays blocked

- **Given** a task blocked behind two dependencies
- **When** one of them is closed
- **Then** it is not unblocked, and still names what it is waiting on

#### a closed task will not take a submit

- **Given** a task that has been closed
- **When** a submit is applied to it
- **Then** it is refused, because a closed task is finished with

#### a closed task will not take a pass

- **Given** a task that has been closed
- **When** a pass is applied to it
- **Then** it is refused, because a closed task is finished with

#### a closed task will not take a fail

- **Given** a task that has been closed
- **When** a fail is applied to it
- **Then** it is refused, because a closed task is finished with

#### a closed task will not take a hold

- **Given** a task that has been closed
- **When** a hold is applied to it
- **Then** it is refused, because a closed task is finished with

#### a closed task will not take a resume

- **Given** a task that has been closed
- **When** a resume is applied to it
- **Then** it is refused, because a closed task is finished with

#### a closed task will not take a feedback

- **Given** a task that has been closed
- **When** a feedback is applied to it
- **Then** it is refused, because a closed task is finished with

#### a closed task will not take an abort

- **Given** a task that has been closed
- **When** an abort is applied to it
- **Then** it is refused, because a closed task is finished with

### Feature: a transition that is refused writes nothing

#### a transition on a task the directory has never held

- **Given** a task directory with nothing in it
- **When** a transition is applied to an id nothing carries
- **Then** it is refused, naming the task that was not found

#### holding with a blank reason is refused

- **Given** a task an agent is working on
- **When** it is held with a reason that is only spaces
- **Then** it is refused, and the task stays where it was

#### every rejected argument leaves the document byte for byte

- **Given** a task an agent is working on, and its document as it stands
- **When** transitions are applied with arguments the machine refuses
- **Then** the document is untouched, so a refusal is never half-applied

### Feature: what changes as a task walks the pipeline

#### the body changes only where a stage hands one in

- **Given** a new task, and the whole pipeline it will be walked through
- **When** the task is walked from new to closed
- **Then** the closed document carries what the last review accepted

#### the clock moves even when the task does not

- **Given** a blocked task, and when it last entered that state
- **When** it is submitted again and stays blocked
- **Then** the clock still moved, so the inbox shows how long it has waited

#### the clock moves when the task moves

- **Given** a blocked task whose dependency has since been edited out
- **When** the task is submitted
- **Then** the clock is stamped with the moment it entered its new state

### Feature: the workspace a task is worked in

#### a task has no workspace before its first claim

- **Given** a task that has entered the pipeline but been dispatched to nobody
- **When** it is submitted into the design phase
- **Then** it carries no workspace, because none has been cloned for it

#### a work claim records the branch, worktree, agent and session

- **Given** a task waiting in the work stage
- **When** a worker claims it with the workspace it was given
- **Then** everything needed to pick the work back up is on the document

#### only the working session is worth recording

- **Given** a task in the design phase
- **When** a designer claims it, naming its own session
- **Then** no session is recorded, because only work is ever resumed

#### a review claim leaves the worker's session where it was

- **Given** a task whose worker recorded a session and then submitted
- **When** a reviewer claims it with a session of its own
- **Then** the worker's session survives, so the work can still be resumed

#### a transition leaves the recorded workspace alone

- **Given** a task claimed with a workspace
- **When** the task moves on
- **Then** the workspace it was cloned into is still recorded on it

#### a work claim with no session records none

- **Given** a task waiting in the work stage
- **When** a worker claims it before its session exists
- **Then** there is no session to resume from, and the field says so

#### a branch with no worktree beside it is refused

- **Given** a task in the design phase
- **When** it is claimed with a branch but no worktree
- **Then** it is refused, and nothing is written onto the task

#### the workspace outlives the claim that recorded it

- **Given** a task whose claiming process is gone but whose workspace is on it
- **When** the claim is cleared
- **Then** the workspace survives, so the next agent can pick the work up

#### closing a task clears the workspace it was worked in

- **Given** a task walked all the way to the manager with a workspace
- **When** the manager closes it
- **Then** the closed document points at no worktree, which is gone by then

#### a workspace missing a field is refused

- **Given** a document whose workspace names only a branch
- **When** it is parsed as a task
- **Then** it is refused, naming the field that is missing

#### a workspace carrying an unknown field is refused

- **Given** a document whose workspace carries a field the schema has no use for
- **When** it is parsed as a task
- **Then** it is refused, naming the field nobody will read

## orchestrator/adapters/tui.test.ts

### Feature: tailing the session an agent is writing

#### only what was appended since the last read is parsed

- **Given** a session file with one turn in it, already read once
- **Given** another turn appended to the file
- **When** the file is read again
- **Then** the new turn joins the old ones rather than replacing them
- **Then** each turn leaves one usage sample behind for the rate meter

#### only the last ten usage samples are kept

- **Given** a session with twelve turns written to it
- **Given** a tail over the session file
- **When** the whole session is read
- **Then** every turn is in the transcript, but the rate is over the recent ones

#### a session rewritten from the start is read afresh

- **Given** a session that has been read, then truncated and written over
- **When** the file is read again
- **Then** the old turns are dropped and only the new session is shown

#### a half written line is held until its newline arrives

- **Given** a session file caught mid-write, with no newline yet
- **Given** the rest of the line, and its newline, written
- **When** the file is read
- **Then** the turn is parsed whole rather than half of it being dropped

#### a session file that is not there yet reads as empty

- **Given** a session path the agent has not written to yet
- **When** the file is read
- **Then** it reads as an empty transcript rather than failing the frame

#### two frames at once never read a turn twice

- **Given** a session file with two turns in it
- **When** the tail is read twice at once
- **Then** each reader sees each turn exactly once

### Feature: the sessions the console is following

#### a session has no rate until its file has been read

- **Given** a session file the console has never read
- **When** the console reads the session
- **Then** the rate is measured from the samples that read left behind

#### a slot with no session at all reads as empty

- **Given** an idle slot, which names no session
- **When** the console asks for its transcript
- **Then** it gets nothing, and no rate either

#### a session no slot names any more is forgotten

- **Given** a session the console has read and measured
- **When** the frame is drawn without that session in it
- **Then** the console drops it, rather than tailing a file nothing shows

### Feature: the command channel between console and server

#### a command the console wrote is taken exactly once

- **Given** a console that has written a command for the server
- **When** the server takes the command twice
- **Then** it arrives once, and is gone the second time

#### a command nobody has taken yet is not written over

- **Given** a command already waiting for the server to take it
- **When** the console tries to write a second command
- **Then** it is refused, and the waiting command survives

#### a command that is not one the server knows is dropped

- **Given** a command file naming something the server cannot do
- **When** the server takes it
- **Then** nothing is applied, and the file is cleared rather than retried

#### an abort command survives the round trip to the server

- **Given** a console that has clicked the abort button on a slot
- **When** the server takes the command
- **Then** it names the slot the click landed on

#### an abort command naming no slot is dropped

- **Given** a command file asking for an abort without saying of what
- **When** the server takes it
- **Then** nothing is applied, and the file is cleared rather than retried

#### a watching server is handed the command as it is written

- **Given** a server watching its runtime directory for commands
- **When** the console writes a command
- **Then** the server is handed it without waiting for a tick, and consumes it

#### a command path that is a directory fails loudly

- **Given** a command path that is a directory, not a file
- **When** the server takes a command from it
- **Then** it fails, rather than pretending nothing was there

#### commands are applied in order, each exactly once

- **Given** a server watching its runtime directory for commands
- **When** the console writes one command after another
- **Then** each arrives once, in the order written

### Feature: reading the views the server publishes

#### the four views the console draws are read together

- **Given** a runtime directory the server has published its views into
- **When** the console reads them
- **Then** it has the agents, the tasks, the checks, the queue and the switch

#### a queue view without the scheduler flag is refused

- **Given** a queue view written without the flag the switch is drawn from
- **When** the console reads the views
- **Then** it says what is missing rather than drawing a switch it cannot trust

#### a runtime directory with no views at all is refused

- **Given** a runtime directory no server has ever written to
- **When** the console reads the views
- **Then** it says there is no server here, rather than drawing an empty screen

#### a view written without its rows is refused

- **Given** a tasks view whose rows are missing
- **When** the console reads the views
- **Then** it names the view and the array it expected to find in it

#### a console with no views to read draws the failure

- **Given** a runtime directory no server has ever written to
- **When** a frame is drawn from it
- **Then** the screen says the console cannot draw, and why

#### a frame joins the views to the sessions they name

- **Given** published views naming a session file the agent has written to
- **When** a frame is drawn from them
- **Then** the pane carries the task, the queue line and the agent's transcript
- **Then** every control the frame drew is a target the console can click

#### a pool of nothing but disabled agents is never hidden

- **Given** views where every agent in the pool has been turned off
- **When** a frame is drawn with the disabled agents collapsed away
- **Then** every pane is drawn, since collapsing them would leave nothing to see

#### collapsing the frame does not forget the hidden panes' sessions

- **Given** published views with a disabled slot still holding a session
- **Given** the console has read the hidden slot's session
- **When** a frame is drawn with that slot collapsed away
- **Then** the session is kept, so un-collapsing does not start from scratch

## orchestrator/adapters/view-files.test.ts

### Feature: reading back the slots the last server published

#### a runtime directory with no view yet has no last slots

- **Given** a runtime directory that no server has published into
- **When** the last slots are read
- **Then** there are none, which is how a first start is told apart

#### the slots come back as the last publish wrote them

- **Given** a server that published a view of its pool
- **When** the next server reads the slots back
- **Then** it gets the rows it needs to reattach to what was running

#### a view file that is not readable JSON stops the server

- **Given** a slots view that was left truncated on disk
- **When** the next server tries to read it back
- **Then** it fails, rather than starting as though nothing had been running

#### a view file carrying no slots stops the server

- **Given** a slots view that parses but holds no slots at all
- **When** the next server tries to read it back
- **Then** it names the file it could not use, and what was missing from it
- **Then** the file really was there, so this is not the first-start path

#### a slots view left by an older server still reattaches

- **Given** a slots view an older server wrote, with keys rows no longer use
- **When** the next server reads the slots back
- **Then** it gets the rows, ignoring the keys it no longer knows

### Feature: publishing the views a console reads

#### a publish replaces every view the console reads

- **Given** a server that has published once
- **When** the next publish replaces them
- **Then** every view carries the sequence number of the same publish

## orchestrator/app/checker.test.ts

### Feature: a check run that cannot finish

#### a task that vanishes mid-run is logged, not thrown into the void

- **Given** a task in CHECK whose document has gone by the time the run starts
- **When** the checker runs it
- **Then** the failure is on the server log rather than an unhandled rejection

#### a transition the graph refuses is logged

- **Given** a task whose checks pass, but which has left CHECK by the time they do
- **When** the checker runs it and its queued transition is refused
- **Then** that too is logged rather than left to crash the server

#### a run is only forgotten once its transition has landed

- **Given** a task in CHECK whose checks all pass
- **When** the checker runs it through to its queued transition
- **Then** it reads as running until the transition lands, so no tick restarts it

## orchestrator/app/pool.test.ts

### Feature: the pool of agent slots

#### every slot in the pool starts idle

- **Given** a pool built from two enabled slots
- **When** the pool is asked for its rows
- **Then** both slots read as idle, because the pool is fixed at load

#### a slot of an agent disabled in the pool file reads as disabled

- **Given** a pool whose only agent is turned off in the pool file
- **When** the pool is asked for its rows
- **Then** the slot reads as disabled rather than idle

#### disabling an agent takes its slots out of the free list

- **Given** a pool with one idle slot
- **When** the agent behind it is disabled
- **Then** nothing is left for the scheduler to dispatch to

#### enabling an agent puts its slots back in the free list

- **Given** a pool whose only agent has been disabled
- **When** the agent is enabled again
- **Then** its slot is dispatchable once more

#### an agent the pool never loaded cannot be toggled

- **Given** a pool that holds one agent
- **When** some other agent is disabled
- **Then** the pool refuses and names what it does hold

### Feature: aborting the tool call an agent is inside

#### a slot running a bash call has that call aborted

- **Given** a slot whose agent is inside a bash tool call
- **When** the slot is aborted
- **Then** the pool records that it killed the command the agent was running

#### a busy slot doing nothing in particular is refused

- **Given** a busy slot whose session reports no activity at all
- **When** the slot is aborted
- **Then** it is refused, because there is no command to kill

#### a slot that is thinking is refused

- **Given** a busy slot that is thinking rather than running a command
- **When** the slot is aborted
- **Then** it is refused, because there is no command to kill

#### a slot that is compacting is refused

- **Given** a busy slot compacting an overflowing context rather than running a command
- **When** the slot is aborted
- **Then** it is refused, because there is no command to kill

#### a slot inside a read call is refused

- **Given** a busy slot reading a file rather than running a command
- **When** the slot is aborted
- **Then** it is refused, because there is no command to kill

#### a slot with no process at all is refused as not running

- **Given** a slot the scheduler has never dispatched to
- **When** that slot is asked to abort its tool call
- **Then** the pool says the slot is not running

#### aborting by agent key rather than slot name is refused

- **Given** a pool whose slots are suffixed with their number
- **When** the agent key is passed where a slot name belongs
- **Then** the pool refuses and lists the slot names it has

### Feature: releasing a slot when its work ends

#### finishing a runner harvests its worktree and returns the slot to idle

- **Given** a slot busy on a task in its own worktree
- **When** the runner is finished with
- **Then** the commits in its worktree are harvested onto its branch
- **Then** the slot reads idle again, holding nothing

#### work that throws stops the slot and says which task it failed on

- **Given** a slot busy on a task
- **When** the work the pool is tracking rejects
- **Then** the failure is logged against the slot and the task it was on
- **Then** the slot is released rather than left holding a broken run

#### a slot is released only once its process is confirmed dead

- **Given** a pool whose process check waits to be answered
- **When** the work the pool is tracking fails, before the process check answers
- **Then** the failure is still settling until the process check answers

#### a pool with nothing tracked has no work in flight

- **Given** a pool that has dispatched nothing
- **When** the amount of work in flight is read
- **Then** there is none, so a tick has nothing to wait on

## orchestrator/app/recovery.test.ts

### Feature: reaping claims whose process is gone

#### a claim held by a dead process is released

- **Given** a task claimed by an agent whose process has exited
- **When** the reaper runs over the graph
- **Then** the claim is cleared, putting the task back in the queue where it stands

#### a claim held by a live process is left alone

- **Given** a task claimed by an agent that is still running
- **When** the reaper runs over the graph
- **Then** nothing is released, because the agent is still working

#### a slot whose own process is alive shields its task from the reaper

- **Given** a task whose recorded pid is gone
- **Given** the slot holding it still has a live process of its own
- **When** the reaper runs over the graph
- **Then** the claim is left alone, because the agent is still running

#### a slot that still holds a dead process is left to the settler

- **Given** a task whose recorded pid is gone
- **Given** the slot holding it kept the process that has already exited
- **When** the reaper runs over the graph
- **Then** it drops neither the claim nor the harvest onto the settler's turn

#### a slot the settler has released stops shielding its task

- **Given** a task whose recorded pid is gone
- **Given** the settler has finished with the slot and handed it back
- **When** the reaper runs over the graph
- **Then** the claim is released and the slot goes back to idle

#### the work a reaped agent committed is harvested onto its branch

- **Given** a dead agent's task with a workspace still on disk
- **When** the reaper runs over the graph
- **Then** the commits in the worktree are harvested before the claim is dropped

### Feature: picking the pool back up after a restart

#### a slot whose pid is still alive is left running

- **Given** a published view naming a slot that is still running a task
- **When** the server reattaches to what the last one left behind
- **Then** the slot is taken as busy on that task rather than dispatched again

#### a slot whose pid is gone is left idle for the scheduler

- **Given** a published view naming a slot whose process has since exited
- **When** the server reattaches to what the last one left behind
- **Then** the slot reads idle, so the scheduler may use it again

#### no view on disk leaves every slot idle

- **Given** a first start, with no published view to read
- **When** the server reattaches to the pool it left behind
- **Then** the whole pool is idle

### Feature: recloning a workspace that went missing

#### a task whose worktree is gone is recloned from its branch

- **Given** a task whose worktree is gone but whose branch survives
- **When** the server recovers its workspaces
- **Then** the worktree is recloned, so the work is not lost

#### a task that lost both its worktree and its branch is only reported

- **Given** a task whose worktree and branch are both gone
- **When** the server recovers its workspaces
- **Then** nothing is recloned, and the loss is written to the log

#### a task whose worktree is still there is left alone

- **Given** a task whose worktree is where it was left
- **When** the server recovers its workspaces
- **Then** nothing is recloned over it

## orchestrator/app/settler.test.ts

### Feature: what the server does with a settled turn

#### an agent that ended without a result is prompted again

- **Given** an agent whose turn ended without calling a result tool
- **When** the server settles that turn
- **Then** it is prompted with the issue rather than the task being moved

#### an agent that keeps ending without a result has its task held

- **Given** an agent that has already used every attempt it is given
- **When** the server settles another turn with no result in it
- **Then** the task is held for a person, saying what the agent never did

#### a settled turn waits for the tick before it touches the graph

- **Given** an agent that has already used every attempt it is given
- **When** its turn settles while nothing is draining the queue
- **Then** the graph is untouched and the slot still holds the task

#### a held task frees the slot that was working on it

- **Given** an agent that has already used every attempt it is given
- **When** the server settles another turn with no result in it
- **Then** the slot goes back to idle rather than being left holding the task

#### an assignment the agent may not have changed is put back

- **Given** an agent that rewrote the part of the assignment it was given
- **When** the server settles that turn
- **Then** what it changed is restored and only its own section survives
- **Then** it is prompted about the rule it broke rather than the task moving

#### an agent whose process died is finished with, not prompted

- **Given** an agent whose process exited without settling its turn
- **When** the server settles that turn
- **Then** nothing is asked of it and the slot goes back to idle

## orchestrator/app/task-graph.test.ts

### Feature: counting the rounds a review has sent work back

#### the round that reaches the limit parks the task

- **Given** a task under review, one round short of the limit that holds it
- **When** a second reviewer sends it back
- **Then** the second round takes it past the limit and parks it

#### a round that is not a review is never counted

- **Given** a task the manager is sending back from outside a review state
- **When** a second round of feedback arrives
- **Then** nothing was counted against it, because only reviews count

## orchestrator/architecture.test.ts

### Feature: the dependency rule

#### no module imports a layer further out than its own

- **Given** every module in the orchestrator, tagged with its layer
- **When** each module's imports are resolved to the layer they land in
- **Then** nothing in an inner layer points outward at all

#### the domain and policy layers reach for no effect at all

- **Given** the two layers that hold the pure decisions
- **Given** those layers are the reason the decisions are testable in isolation
- **When** each is searched for a filesystem, subprocess, network or environment effect
- **Then** none of them reaches for one

#### no test in an inner layer reaches for the filesystem

- **Given** every suite in the layers that only decide
- **Given** there are enough of them for the rule to mean something
- **When** each is searched for a filesystem effect or the temp directory rig
- **Then** none of them needs a repository, a task directory or a subprocess

#### the only modules outside a layer are the extensions and the test rig

- **Given** the modules that belong to no layer directory
- **When** their names are collected
- **Then** each is either a pi extension, which pi loads by path, or test scaffolding

## orchestrator/bdd.test.ts

### Feature: behaviour tests are written as Given, When, Then

#### every suite in the orchestrator is read by this check

- **Given** every test file on disk
- **When** the tests this check can read are counted against them
- **Then** none of them is skipped, so the rules below cover the whole suite

#### every test states a Given, a When and a Then

- **Given** every test in the orchestrator
- **Given** there are enough of them for the rule to mean something
- **When** each is checked for the three comments
- **Then** none of them is missing one

#### every test has exactly one When, being one behaviour

- **Given** every test in the orchestrator
- **When** the tests naming more than one behaviour are collected
- **Then** none of them tests two behaviours at once

#### every Given, When and Then reads as a sentence about the system

- **Given** every comment in every suite
- **When** the ones too short to be a sentence, or written as code, are collected
- **Then** every one of them is prose describing behaviour

#### no test stands for a table of rows

- **Given** every test file on disk
- **When** the suites declaring their tests from a table are collected
- **Then** none of them does, because a row leaves the Given, When and Then abstract

#### every suite names the feature it covers

- **Given** every test file on disk
- **When** each is read for its describe block
- **Then** each one is introduced as a feature

## orchestrator/domain/assignment.test.ts

### Feature: what an agent may write in its assignment

#### an assignment that came back untouched reads as unchanged

- **Given** an assignment the agent appended nothing to
- **When** it is compared with what was dispatched
- **Then** the server sees an agent that wrote nothing

#### an assignment with an appended section reads as an ordinary append

- **Given** an assignment the agent appended its design to
- **When** it is compared with what was dispatched
- **Then** the append is allowed

#### an assignment edited above the section reads as modified

- **Given** an assignment whose body the agent rewrote
- **When** it is compared with what was dispatched
- **Then** the server sees an agent that wrote where it may not

### Feature: restoring an assignment an agent modified

#### what the agent wrote under its own section survives the restore

- **Given** an agent that edited the body and appended a design
- **When** the assignment is restored around its section
- **Then** the dispatched body comes back
- **Then** the agent's own design is still there

#### a section the agent renamed away leaves nothing to keep

- **Given** an agent that renamed the heading it was told to write under
- **When** the assignment is restored around its section
- **Then** the file is exactly what was dispatched

#### the last heading is taken as the one the agent wrote under

- **Given** a body that mentions the heading before the agent's real one
- **When** the assignment is restored around its section
- **Then** only what follows the last heading is kept

#### a reviewer keeps nothing, having no section of its own

- **Given** a reviewer that appended a section it was never given
- **When** the assignment is restored with no section to keep
- **Then** the whole file goes back to what was dispatched

## orchestrator/domain/exclusive-lock.test.ts

### Feature: a lock that hands one caller the shared state at a time

#### a caller still inside keeps everyone else out

- **Given** a lock with the value "a"
- **Given** the first caller with the lock read the value then updated it "b"
- **Given** a second caller waits for the lock to read the value
- **When** the first caller releases the lock
- **Then** we see the read order as "a", then "b"

#### a caller that fails still frees the lock

- **Given** a lock
- **When** the lock is acquired but an error is thrown
- **Then** the lock is released and can be acquired

## orchestrator/domain/latch.test.ts

### Feature: a latch that holds on to the wake-up nobody heard

#### everyone waiting is woken by one notify

- **Given** two callers waiting on a latch
- **When** the latch is notified once
- **Then** both were woken

#### a notify nobody was waiting for is kept for the next waiter

- **Given** a latch notified while nothing was waiting
- **When** a caller waits afterwards
- **Then** it does not wait at all

#### clearing the latch puts the next waiter back to waiting

- **Given** a latch whose notify has been taken
- **When** a caller waits
- **Then** it waits for the next notify

#### a waiter that gives up is released without a notify

- **Given** a caller that waits with a signal it controls
- **When** it gives up
- **Then** it is released, and the latch was never raised for anyone else

#### a caller that has already given up never waits

- **Given** a signal that is aborted before the wait
- **When** a caller waits on it
- **Then** it returns right away

## orchestrator/domain/queue.test.ts

### Feature: work handed to the tick to run one job at a time

#### submitted work sits still until the queue is drained

- **Given** a queue holding submitted work
- **Given** nothing has run
- **When** the queue is drained
- **Then** the work ran and the submitter has its answer

#### a job still running holds up the ones behind it

- **Given** a queue draining a job that has not finished
- **Given** the job behind it has not started
- **When** the running job finishes
- **Then** the job behind it ran next

#### a job that fails is the only one that fails

- **Given** a failing job submitted before a healthy one
- **When** the queue is drained
- **Then** the failure reached its submitter and the next job still ran

#### work a job submits waits for the next drain

- **Given** a job that submits more work as it runs
- **Given** the first drain ran only the job that was waiting
- **When** the queue is drained again
- **Then** the work that job submitted ran

#### a waiter is woken by the work that arrives

- **Given** a caller waiting for work to arrive
- **When** work is submitted
- **Then** the waiter is woken, so a drain can be run for it

#### work that arrives with nobody waiting still wakes the next waiter

- **Given** work submitted while nothing was waiting on the queue
- **When** a caller waits afterwards
- **Then** it is woken by the work already sitting there

#### a drained queue leaves the next waiter waiting

- **Given** a queue whose work has been drained
- **When** a caller waits for more work to arrive
- **Then** it waits, because the work it would have woken for is done

#### closing the queue fails the work nobody will run

- **Given** a queue holding work
- **When** the queue closes
- **Then** the waiting work fails, and so does anything submitted after

## orchestrator/domain/rates.test.ts

### Feature: measuring how fast an agent writes

#### an agent that has produced nothing has no rate

- **Given** no samples at all
- **When** the rate is measured
- **Then** there is no rate to report

#### a single sample is measured from when it arrived

- **Given** one message of five tokens, five seconds ago
- **When** the rate is measured
- **Then** it is one token per second

#### two samples are measured over the span between them

- **Given** two messages a second apart carrying ten tokens together
- **When** the rate is measured long after both arrived
- **Then** the idle time since is not counted against the agent

#### samples that share a timestamp fall back to measuring until now

- **Given** two messages stamped at the same instant
- **When** the rate is measured two seconds later
- **Then** the span is taken from the first sample to now

#### a sample stamped in the future gives no rate

- **Given** a message whose clock ran ahead of ours
- **When** the rate is measured
- **Then** there is no rate rather than a negative one

### Feature: the rate table the scheduler picks slots by

#### an agent nobody has run yet has no rate

- **Given** a table nothing has been recorded into
- **When** an agent's rate is read
- **Then** it has none, which the scheduler reads as untried

#### an agent's rate is its output tokens over the span of its messages

- **Given** an agent that produced twenty tokens in each of two messages
- **When** its rate is read
- **Then** forty tokens over one second is forty per second

#### only the last ten messages count towards the rate

- **Given** an agent whose first message was enormous
- **Given** ten ordinary messages after it, one a second
- **When** its rate is read
- **Then** the enormous message has fallen out of the window

#### slots of the same model share one window

- **Given** two messages recorded against one agent key
- **When** its rate is read
- **Then** the samples belong to the agent, not to the slot that produced them
- **Then** a different agent is still untried

## orchestrator/domain/session.test.ts

### Feature: reading a session record

#### an assistant turn becomes one entry per thought, message and tool call

- **Given** an assistant turn that thought, spoke and called a tool
- **When** the record is read into transcript entries
- **Then** each part of the turn is one labelled entry, in the order it happened
- **Then** every entry carries the moment the turn was written
- **Then** the tokens the turn cost come back with it, for the rate meter

#### a tool result that ran to several lines collapses to a line count

- **Given** a tool result with three lines of output
- **When** the record is read into transcript entries
- **Then** the console is given the size of the output rather than the output

#### a tool result that failed is flagged and shows its first line

- **Given** a tool result the agent's command failed on
- **When** the record is read into transcript entries
- **Then** the entry is marked as an error and reads as the first line of it

#### a record the console has no use for produces nothing

- **Given** a session record of a kind the console does not draw
- **When** the record is read into transcript entries
- **Then** it contributes neither an entry nor a usage sample

#### the prompt an agent was given becomes one user entry

- **Given** a session record holding the prompt the server sent the agent
- **When** the record is read into transcript entries
- **Then** the prompt is shown as the agent's own turn, labelled as the user

#### a prompt with nothing in it is not drawn at all

- **Given** a session record whose user message carries no text
- **When** the record is read into transcript entries
- **Then** the console is given no entry to draw for it

#### the start of a session says where the agent is working

- **Given** the record that opens every session file
- **When** the record is read into transcript entries
- **Then** the transcript opens by naming the working directory

#### a session that opens without a working directory says so

- **Given** a session record that names no working directory
- **When** the record is read into transcript entries
- **Then** the entry still draws, with the directory marked unknown

#### changing model is announced as the provider and the model

- **Given** a record saying the session moved to another model
- **When** the record is read into transcript entries
- **Then** the transcript names both halves of what it moved to

#### changing the thinking level is announced against the model

- **Given** a record saying the session changed how hard it thinks
- **When** the record is read into transcript entries
- **Then** the change is labelled as a model change too

#### a role the console does not know produces nothing

- **Given** a message from a role the transcript has no row for
- **When** the record is read into transcript entries
- **Then** it contributes neither an entry nor a usage sample

#### a record whose message is not an object is ignored

- **Given** a message record written with a string where the message goes
- **When** the record is read into transcript entries
- **Then** the console draws nothing rather than failing on it

#### an assistant turn that reports no tokens still draws

- **Given** an assistant turn whose record carries no usage at all
- **When** the record is read into transcript entries
- **Then** the turn is drawn, and the rate meter is given no sample

#### a tool call names the argument worth reading

- **Given** an assistant turn calling a tool with a path among its arguments
- **When** the record is read into transcript entries
- **Then** the row shows the path rather than the whole argument object

#### a tool call with no argument worth reading shows all of them

- **Given** an assistant turn calling a tool with only arguments it cannot rank
- **When** the record is read into transcript entries
- **Then** the row falls back to the arguments as they were written

#### a tool call whose arguments are not an object still draws a row

- **Given** an assistant turn calling a tool with a bare string for arguments
- **When** the record is read into transcript entries
- **Then** the argument is shown as it stands, named by the tool that took it

#### a tool result of nothing but blank lines reads as empty

- **Given** a tool result whose only output is whitespace
- **When** the record is read into transcript entries
- **Then** the row is drawn with no text rather than with the whitespace

### Feature: gathering session records into a transcript

#### a thinking level change is folded onto the model it belongs to

- **Given** a transcript whose last entry is the model the session moved to
- **When** the thinking level that came with it is gathered in
- **Then** the two are drawn as one row rather than two

#### a thinking level change after other work stands on its own

- **Given** a transcript whose last entry is something the agent said
- **When** a thinking level change is gathered in after it
- **Then** it is drawn as its own row, having no model row to join

## orchestrator/domain/state-machine.test.ts

### Feature: where every transition lands

#### a task in NEW that submits lands in DESIGN

- **Given** a new task with nothing holding it back
- **When** a submit is decided on it
- **Then** the task lands in DESIGN

#### a task in BLOCKED that submits lands in DESIGN

- **Given** a blocked task with nothing left to wait on
- **When** a submit is decided on it
- **Then** the task lands in DESIGN

#### a task in HELD_DESIGN that resumes lands in DESIGN

- **Given** a task parked on a design hold
- **When** a resume is decided on it
- **Then** the task lands in DESIGN

#### a task in HELD_DESIGN that aborts lands in CLOSED

- **Given** a task parked on a design hold
- **When** an abort is decided on it
- **Then** the task lands in CLOSED

#### a task in HELD_PLAN that resumes lands in PLAN

- **Given** a task parked on a plan hold
- **When** a resume is decided on it
- **Then** the task lands in PLAN

#### a task in HELD_PLAN that aborts lands in CLOSED

- **Given** a task parked on a plan hold
- **When** an abort is decided on it
- **Then** the task lands in CLOSED

#### a task in HELD_WORK that resumes lands in WORK

- **Given** a task parked on a work hold
- **When** a resume is decided on it
- **Then** the task lands in WORK

#### a task in HELD_WORK that aborts lands in CLOSED

- **Given** a task parked on a work hold
- **When** an abort is decided on it
- **Then** the task lands in CLOSED

#### a task in DESIGN that submits lands in DESIGN_REVIEW

- **Given** a task in the design stage
- **When** a submit is decided on it
- **Then** the task lands in DESIGN_REVIEW

#### a task in DESIGN that holds lands in HELD_DESIGN

- **Given** a task in the design stage
- **When** a hold is decided on it
- **Then** the task lands in HELD_DESIGN

#### a task in DESIGN_REVIEW that submits lands in PLAN

- **Given** a task sitting in design review
- **When** a submit is decided on it
- **Then** the task lands in PLAN

#### a task in DESIGN_REVIEW that sends feedback lands in DESIGN

- **Given** a task sitting in design review
- **When** feedback is decided on it
- **Then** the task lands in DESIGN

#### a task in DESIGN_REVIEW that holds lands in HELD_DESIGN

- **Given** a task sitting in design review
- **When** a hold is decided on it
- **Then** the task lands in HELD_DESIGN

#### a task in PLAN that submits lands in PLAN_REVIEW

- **Given** a task in the plan stage
- **When** a submit is decided on it
- **Then** the task lands in PLAN_REVIEW

#### a task in PLAN that holds lands in HELD_PLAN

- **Given** a task in the plan stage
- **When** a hold is decided on it
- **Then** the task lands in HELD_PLAN

#### a task in PLAN_REVIEW that submits lands in WORK

- **Given** a task sitting in plan review
- **When** a submit is decided on it
- **Then** the task lands in WORK

#### a task in PLAN_REVIEW that sends feedback lands in PLAN

- **Given** a task sitting in plan review
- **When** feedback is decided on it
- **Then** the task lands in PLAN

#### a task in PLAN_REVIEW that holds lands in HELD_PLAN

- **Given** a task sitting in plan review
- **When** a hold is decided on it
- **Then** the task lands in HELD_PLAN

#### a task in WORK that submits lands in CHECK

- **Given** a task being worked on
- **When** a submit is decided on it
- **Then** the task lands in CHECK

#### a task in WORK that holds lands in HELD_WORK

- **Given** a task being worked on
- **When** a hold is decided on it
- **Then** the task lands in HELD_WORK

#### a task in CHECK that passes lands in WORK_REVIEW

- **Given** a task sitting in the check stage
- **When** a pass is decided on it
- **Then** the task lands in WORK_REVIEW

#### a task in CHECK that fails lands in WORK

- **Given** a task sitting in the check stage
- **When** a fail is decided on it
- **Then** the task lands in WORK

#### a task in CHECK that holds lands in HELD_WORK

- **Given** a task sitting in the check stage
- **When** a hold is decided on it
- **Then** the task lands in HELD_WORK

#### a task in WORK_REVIEW that submits lands in MANAGER_REVIEW

- **Given** a task sitting in work review
- **When** a submit is decided on it
- **Then** the task lands in MANAGER_REVIEW

#### a task in WORK_REVIEW that sends feedback lands in WORK

- **Given** a task sitting in work review
- **When** feedback is decided on it
- **Then** the task lands in WORK

#### a task in WORK_REVIEW that holds lands in HELD_WORK

- **Given** a task sitting in work review
- **When** a hold is decided on it
- **Then** the task lands in HELD_WORK

#### a task in MANAGER_REVIEW that submits lands in CLOSED

- **Given** a task waiting on the manager
- **When** a submit is decided on it
- **Then** the task lands in CLOSED

#### a task in MANAGER_REVIEW that sends feedback lands in WORK

- **Given** a task waiting on the manager
- **When** feedback is decided on it
- **Then** the task lands in WORK

#### a task in MANAGER_REVIEW that aborts lands in CLOSED

- **Given** a task waiting on the manager
- **When** an abort is decided on it
- **Then** the task lands in CLOSED

### Feature: the transitions a task turns away

#### a task in NEW will not take a pass

- **Given** a new task with nothing holding it back
- **When** a pass is decided on it
- **Then** it is turned away as not valid from NEW

#### a task in NEW will not take a fail

- **Given** a new task with nothing holding it back
- **When** a fail is decided on it
- **Then** it is turned away as not valid from NEW

#### a task in NEW will not take a hold

- **Given** a new task with nothing holding it back
- **When** a hold is decided on it
- **Then** it is turned away as not valid from NEW

#### a task in NEW will not take a resume

- **Given** a new task with nothing holding it back
- **When** a resume is decided on it
- **Then** it is turned away as not valid from NEW

#### a task in NEW will not take feedback

- **Given** a new task with nothing holding it back
- **When** feedback is decided on it
- **Then** it is turned away as not valid from NEW

#### a task in NEW will not take an abort

- **Given** a new task with nothing holding it back
- **When** an abort is decided on it
- **Then** it is turned away as not valid from NEW

#### a task in BLOCKED will not take a pass

- **Given** a blocked task with nothing left to wait on
- **When** a pass is decided on it
- **Then** it is turned away as not valid from BLOCKED

#### a task in BLOCKED will not take a fail

- **Given** a blocked task with nothing left to wait on
- **When** a fail is decided on it
- **Then** it is turned away as not valid from BLOCKED

#### a task in BLOCKED will not take a hold

- **Given** a blocked task with nothing left to wait on
- **When** a hold is decided on it
- **Then** it is turned away as not valid from BLOCKED

#### a task in BLOCKED will not take a resume

- **Given** a blocked task with nothing left to wait on
- **When** a resume is decided on it
- **Then** it is turned away as not valid from BLOCKED

#### a task in BLOCKED will not take feedback

- **Given** a blocked task with nothing left to wait on
- **When** feedback is decided on it
- **Then** it is turned away as not valid from BLOCKED

#### a task in BLOCKED will not take an abort

- **Given** a blocked task with nothing left to wait on
- **When** an abort is decided on it
- **Then** it is turned away as not valid from BLOCKED

#### a task in HELD_DESIGN will not take a submit

- **Given** a task parked on a design hold
- **When** a submit is decided on it
- **Then** it is turned away as not valid from HELD_DESIGN

#### a task in HELD_DESIGN will not take a pass

- **Given** a task parked on a design hold
- **When** a pass is decided on it
- **Then** it is turned away as not valid from HELD_DESIGN

#### a task in HELD_DESIGN will not take a fail

- **Given** a task parked on a design hold
- **When** a fail is decided on it
- **Then** it is turned away as not valid from HELD_DESIGN

#### a task in HELD_DESIGN will not take a hold

- **Given** a task parked on a design hold
- **When** a hold is decided on it
- **Then** it is turned away as not valid from HELD_DESIGN

#### a task in HELD_DESIGN will not take feedback

- **Given** a task parked on a design hold
- **When** feedback is decided on it
- **Then** it is turned away as not valid from HELD_DESIGN

#### a task in HELD_PLAN will not take a submit

- **Given** a task parked on a plan hold
- **When** a submit is decided on it
- **Then** it is turned away as not valid from HELD_PLAN

#### a task in HELD_PLAN will not take a pass

- **Given** a task parked on a plan hold
- **When** a pass is decided on it
- **Then** it is turned away as not valid from HELD_PLAN

#### a task in HELD_PLAN will not take a fail

- **Given** a task parked on a plan hold
- **When** a fail is decided on it
- **Then** it is turned away as not valid from HELD_PLAN

#### a task in HELD_PLAN will not take a hold

- **Given** a task parked on a plan hold
- **When** a hold is decided on it
- **Then** it is turned away as not valid from HELD_PLAN

#### a task in HELD_PLAN will not take feedback

- **Given** a task parked on a plan hold
- **When** feedback is decided on it
- **Then** it is turned away as not valid from HELD_PLAN

#### a task in HELD_WORK will not take a submit

- **Given** a task parked on a work hold
- **When** a submit is decided on it
- **Then** it is turned away as not valid from HELD_WORK

#### a task in HELD_WORK will not take a pass

- **Given** a task parked on a work hold
- **When** a pass is decided on it
- **Then** it is turned away as not valid from HELD_WORK

#### a task in HELD_WORK will not take a fail

- **Given** a task parked on a work hold
- **When** a fail is decided on it
- **Then** it is turned away as not valid from HELD_WORK

#### a task in HELD_WORK will not take a hold

- **Given** a task parked on a work hold
- **When** a hold is decided on it
- **Then** it is turned away as not valid from HELD_WORK

#### a task in HELD_WORK will not take feedback

- **Given** a task parked on a work hold
- **When** feedback is decided on it
- **Then** it is turned away as not valid from HELD_WORK

#### a task in DESIGN will not take a pass

- **Given** a task in the design stage
- **When** a pass is decided on it
- **Then** it is turned away as not valid from DESIGN

#### a task in DESIGN will not take a fail

- **Given** a task in the design stage
- **When** a fail is decided on it
- **Then** it is turned away as not valid from DESIGN

#### a task in DESIGN will not take a resume

- **Given** a task in the design stage
- **When** a resume is decided on it
- **Then** it is turned away as not valid from DESIGN

#### a task in DESIGN will not take feedback

- **Given** a task in the design stage
- **When** feedback is decided on it
- **Then** it is turned away as not valid from DESIGN

#### a task in DESIGN will not take an abort

- **Given** a task in the design stage
- **When** an abort is decided on it
- **Then** it is turned away as not valid from DESIGN

#### a task in DESIGN_REVIEW will not take a pass

- **Given** a task sitting in design review
- **When** a pass is decided on it
- **Then** it is turned away as not valid from DESIGN_REVIEW

#### a task in DESIGN_REVIEW will not take a fail

- **Given** a task sitting in design review
- **When** a fail is decided on it
- **Then** it is turned away as not valid from DESIGN_REVIEW

#### a task in DESIGN_REVIEW will not take a resume

- **Given** a task sitting in design review
- **When** a resume is decided on it
- **Then** it is turned away as not valid from DESIGN_REVIEW

#### a task in DESIGN_REVIEW will not take an abort

- **Given** a task sitting in design review
- **When** an abort is decided on it
- **Then** it is turned away as not valid from DESIGN_REVIEW

#### a task in PLAN will not take a pass

- **Given** a task in the plan stage
- **When** a pass is decided on it
- **Then** it is turned away as not valid from PLAN

#### a task in PLAN will not take a fail

- **Given** a task in the plan stage
- **When** a fail is decided on it
- **Then** it is turned away as not valid from PLAN

#### a task in PLAN will not take a resume

- **Given** a task in the plan stage
- **When** a resume is decided on it
- **Then** it is turned away as not valid from PLAN

#### a task in PLAN will not take feedback

- **Given** a task in the plan stage
- **When** feedback is decided on it
- **Then** it is turned away as not valid from PLAN

#### a task in PLAN will not take an abort

- **Given** a task in the plan stage
- **When** an abort is decided on it
- **Then** it is turned away as not valid from PLAN

#### a task in PLAN_REVIEW will not take a pass

- **Given** a task sitting in plan review
- **When** a pass is decided on it
- **Then** it is turned away as not valid from PLAN_REVIEW

#### a task in PLAN_REVIEW will not take a fail

- **Given** a task sitting in plan review
- **When** a fail is decided on it
- **Then** it is turned away as not valid from PLAN_REVIEW

#### a task in PLAN_REVIEW will not take a resume

- **Given** a task sitting in plan review
- **When** a resume is decided on it
- **Then** it is turned away as not valid from PLAN_REVIEW

#### a task in PLAN_REVIEW will not take an abort

- **Given** a task sitting in plan review
- **When** an abort is decided on it
- **Then** it is turned away as not valid from PLAN_REVIEW

#### a task in WORK will not take a pass

- **Given** a task being worked on
- **When** a pass is decided on it
- **Then** it is turned away as not valid from WORK

#### a task in WORK will not take a fail

- **Given** a task being worked on
- **When** a fail is decided on it
- **Then** it is turned away as not valid from WORK

#### a task in WORK will not take a resume

- **Given** a task being worked on
- **When** a resume is decided on it
- **Then** it is turned away as not valid from WORK

#### a task in WORK will not take feedback

- **Given** a task being worked on
- **When** feedback is decided on it
- **Then** it is turned away as not valid from WORK

#### a task in WORK will not take an abort

- **Given** a task being worked on
- **When** an abort is decided on it
- **Then** it is turned away as not valid from WORK

#### a task in CHECK will not take a submit

- **Given** a task sitting in the check stage
- **When** a submit is decided on it
- **Then** it is turned away as not valid from CHECK

#### a task in CHECK will not take a resume

- **Given** a task sitting in the check stage
- **When** a resume is decided on it
- **Then** it is turned away as not valid from CHECK

#### a task in CHECK will not take feedback

- **Given** a task sitting in the check stage
- **When** feedback is decided on it
- **Then** it is turned away as not valid from CHECK

#### a task in CHECK will not take an abort

- **Given** a task sitting in the check stage
- **When** an abort is decided on it
- **Then** it is turned away as not valid from CHECK

#### a task in WORK_REVIEW will not take a pass

- **Given** a task sitting in work review
- **When** a pass is decided on it
- **Then** it is turned away as not valid from WORK_REVIEW

#### a task in WORK_REVIEW will not take a fail

- **Given** a task sitting in work review
- **When** a fail is decided on it
- **Then** it is turned away as not valid from WORK_REVIEW

#### a task in WORK_REVIEW will not take a resume

- **Given** a task sitting in work review
- **When** a resume is decided on it
- **Then** it is turned away as not valid from WORK_REVIEW

#### a task in WORK_REVIEW will not take an abort

- **Given** a task sitting in work review
- **When** an abort is decided on it
- **Then** it is turned away as not valid from WORK_REVIEW

#### a task in MANAGER_REVIEW will not take a pass

- **Given** a task waiting on the manager
- **When** a pass is decided on it
- **Then** it is turned away as not valid from MANAGER_REVIEW

#### a task in MANAGER_REVIEW will not take a fail

- **Given** a task waiting on the manager
- **When** a fail is decided on it
- **Then** it is turned away as not valid from MANAGER_REVIEW

#### a task in MANAGER_REVIEW will not take a hold

- **Given** a task waiting on the manager
- **When** a hold is decided on it
- **Then** it is turned away as not valid from MANAGER_REVIEW

#### a task in MANAGER_REVIEW will not take a resume

- **Given** a task waiting on the manager
- **When** a resume is decided on it
- **Then** it is turned away as not valid from MANAGER_REVIEW

### Feature: the edges that read the task itself

#### a new task with dependencies is submitted into BLOCKED

- **Given** a new task that depends on another
- **When** the task is submitted into the pipeline
- **Then** it waits in BLOCKED rather than entering the design phase

#### a blocked task that still has dependencies stays where it is

- **Given** a blocked task whose dependency has not closed
- **When** the task is submitted into the pipeline
- **Then** the machine moves it nowhere at all

#### a held task that gained a dependency resumes into BLOCKED

- **Given** a held task that was given a dependency while it was parked
- **When** the task is resumed off its hold
- **Then** it waits in BLOCKED instead of returning to its phase

### Feature: what a transition carries with it

#### holding a task records the reason it was parked

- **Given** a task an agent is working on
- **When** it is held with a reason
- **Then** the reason is written onto the task

#### holding a task without a reason is refused

- **Given** a task an agent is working on
- **When** it is held with no reason given
- **Then** the machine insists on a reason

#### a review that accepts the work carries the reviewed body forward

- **Given** a reviewer holding a task in design review
- **When** it submits the body it reviewed
- **Then** the body it submitted is what the task will carry

#### a review that accepts the work without a body is refused

- **Given** a reviewer holding a task in design review
- **When** it submits with no body
- **Then** the machine insists on the body it reviewed

#### a stage that writes no body submits without carrying one

- **Given** a designer holding a task in the design stage
- **When** the designer submits its stage
- **Then** the machine carries no body forward

#### findings sent back to the worker are appended to the task body

- **Given** a reviewer holding a task in work review
- **When** it sends findings back
- **Then** the findings are appended to the body the worker will read

#### findings sent back to an earlier phase leave the body alone

- **Given** a reviewer holding a task in plan review
- **When** it sends findings back to the planner
- **Then** the body is left for the planner to rewrite

#### feedback with no findings is refused

- **Given** a reviewer holding a task in work review
- **When** it sends feedback with an empty list of findings
- **Then** the machine insists on at least one finding

### Feature: only the agent holding a task may speak for it

#### an unclaimed task in an agent state cannot be submitted

- **Given** a task in an agent state that nothing is claiming
- **When** a submit arrives for it
- **Then** the machine refuses it as an agent speaking for a task it does not hold

#### an unclaimed task in an agent state cannot be sent feedback

- **Given** a task in review that nothing is claiming
- **When** feedback arrives for it
- **Then** the machine refuses it as an agent speaking for a task it does not hold

#### a manager may still speak for a task no agent holds

- **Given** a task waiting on the manager, which no agent claims
- **When** the manager submits it
- **Then** the task closes, because the rule guards agent states only

## orchestrator/domain/text.test.ts

### Feature: how wide a piece of text draws

#### an ascii letter draws in a single column

- **Given** a plain ascii letter
- **When** it is measured in terminal columns
- **Then** it takes the one column a narrow character takes

#### an ellipsis draws in a single column

- **Given** the ellipsis character on its own
- **When** it is measured in terminal columns
- **Then** it takes one column

#### a bare checkmark draws in a single column

- **Given** the checkmark character on its own
- **When** it is measured in terminal columns
- **Then** it takes one column

#### a checkmark with a variation selector draws wide

- **Given** a checkmark followed by the emoji variation selector
- **When** it is measured in terminal columns
- **Then** it takes two columns

#### a fire emoji draws wide

- **Given** the fire emoji on its own
- **When** it is measured in terminal columns
- **Then** it takes two columns

#### a cjk character draws wide

- **Given** a cjk character on its own
- **When** it is measured in terminal columns
- **Then** it takes two columns

#### a keycap draws wide

- **Given** the keycap emoji, made of a digit and a combining cap
- **When** it is measured in terminal columns
- **Then** it takes two columns

#### a family built from several code points draws as one wide cluster

- **Given** a family emoji joined from several code points
- **When** it is measured in terminal columns
- **Then** it takes two columns, not one for each code point

#### a flag built from two regional indicators draws as one wide cluster

- **Given** a flag made of two joined regional indicators
- **When** it is measured in terminal columns
- **Then** it takes two columns, not one for each indicator

#### the width of a string counts columns rather than code points

- **Given** a string with an emoji in it
- **When** the string is measured
- **Then** the emoji is counted as the two columns it draws in

### Feature: clipping a line to the width it has

#### a line too long for its pane ends in an ellipsis

- **Given** a line wider than the pane it must fit
- **When** it is clipped to eight columns
- **Then** it ends in an ellipsis and fills exactly the width it was given
- **Then** every piece of it keeps the colour it was written in

#### a line that fits exactly is left as it was

- **Given** a line exactly as wide as its pane
- **When** it is clipped to that width
- **Then** nothing is cut and no ellipsis is added

#### a pane with no width at all draws nothing

- **Given** a line and a pane with no room in it
- **When** the line is clipped to no columns
- **Then** nothing is drawn, rather than an ellipsis on its own

#### an emoji is never allowed to spill past the width

- **Given** a line whose emoji straddles the column it would be cut at
- **When** it is clipped to four columns
- **Then** the emoji is dropped whole, and the line stays inside its pane

#### emoji that fill the width exactly are all kept

- **Given** a line of two emoji, four columns wide
- **When** it is clipped to four columns
- **Then** both are kept, because neither straddles the edge

### Feature: padding a line out to its pane

#### a short line is filled out to the width of the pane

- **Given** a line narrower than its pane
- **When** it is padded to six columns
- **Then** it fills the pane, so the pane beside it starts where it should

#### a long line is cut to the width of the pane

- **Given** a line wider than its pane
- **When** it is padded to six columns
- **Then** it is cut back to the pane rather than overflowing it

#### the column a clipped emoji could not use is filled with a space

- **Given** a line whose emoji cannot fit in the columns left
- **When** it is padded to four columns
- **Then** the pane is still filled exactly, with a space where the emoji was

### Feature: splitting a line at a column

#### taking a prefix cuts at the column with no ellipsis

- **Given** a line wider than the cut
- **When** the first five columns are taken
- **Then** the text is simply cut, because this is a split and not a clip

#### taking a prefix across two spans keeps their colours

- **Given** a line of two spans written in different colours
- **When** the first three columns are taken
- **Then** each piece keeps the colour of the span it came from

#### an emoji straddling the cut is replaced by the space it left

- **Given** a line whose emoji straddles the column being cut at
- **When** the first three columns are taken
- **Then** the emoji is dropped and its half column becomes a space

#### dropping a prefix keeps everything past the column

- **Given** a line wider than the cut
- **When** the first six columns are dropped
- **Then** what is left is everything after them

#### dropping a whole span leaves only the spans after it

- **Given** a line of two spans
- **When** the columns of the first span are dropped
- **Then** only the second span is left, untouched

#### an emoji straddling the drop is replaced by the space it left

- **Given** a line whose emoji straddles the column being cut at
- **When** the first three columns are dropped
- **Then** the emoji is dropped and its half column becomes a space

#### the two halves of a split always add up to the whole line

- **Given** a line with an emoji in it, split at every column in turn
- **When** each split is measured on both sides
- **Then** no column is ever lost or gained, wherever the cut falls

### Feature: turning a line into what the terminal reads

#### a coloured span is wrapped in escapes and plain text is left alone

- **Given** a line with one coloured span and one plain one
- **When** the line is rendered for the terminal
- **Then** only the coloured span carries escapes, and it closes them again

### Feature: wrapping a transcript entry

#### text wraps on word boundaries, with a narrower first line

- **Given** an entry whose first line is shortened by its timestamp and label
- **When** it is wrapped to seven columns first and eleven after
- **Then** it breaks between words rather than inside them

#### a word longer than the pane is split rather than overflowing

- **Given** a single word wider than the pane
- **When** it is wrapped to four columns
- **Then** it is broken across as many lines as it needs

#### an entry with no text still takes up one line

- **Given** an entry with nothing in it
- **When** the empty entry is wrapped to the pane
- **Then** it still occupies a line, so the transcript keeps its shape

#### wrapping counts emoji columns rather than code points

- **Given** text whose emoji fill the pane
- **When** it is wrapped to four columns
- **Then** the break falls where the columns run out, not where the words do

#### an emoji wider than the pane still makes progress

- **Given** a pane one column wide and emoji two columns wide
- **When** the text is wrapped to that pane
- **Then** each emoji takes a line of its own rather than looping forever

## orchestrator/domain/workspace.test.ts

### Feature: the branch a task's work lands on

#### a task's branch is named for the task it does

- **Given** the id of a task
- **When** its branch name is worked out
- **Then** it names the task, so the branch reads as work in the repository

## orchestrator/main/mcp.test.ts

### Feature: the tool surface the manager works through

#### the manager gets one tool per judgement it can make

- **Given** a server running over stdio against a project
- **When** the manager asks what it can do
- **Then** there is one tool for each judgement only a person can make

#### every view the console draws is readable as a resource

- **Given** a server running over stdio against a project
- **When** the manager asks what it can read
- **Then** every view the console draws is there to be read

#### the manager is told where the graph, the pool and the prompts are

- **Given** a server running over stdio against a project
- **When** the manager reads the paths resource
- **Then** the graph, the pool and the prompts are each named in it

#### task_feedback sends the task back with the findings in the body and the findings file

- **Given** a task that has reached the manager for review
- **When** the manager sends it back with a finding
- **Then** the task goes back to work with the finding written into its body
- **Then** the finding is also left where the next dispatch will read it

#### reload_prompts picks up an override written after startup

- **Given** a running server whose project overrides no prompts yet
- **Given** an override written after the server started
- **When** the prompts are reloaded
- **Then** the new file is cached, without the server being restarted

#### task_create returns a path the manager can edit directly

- **Given** a project with no tasks in it yet
- **When** the manager creates one
- **Then** it takes the first id, and the manager is told which file to edit

#### task_write_body replaces the body and leaves the frontmatter alone

- **Given** a task an agent is already working on
- **When** the manager rewrites its body
- **Then** the prose is replaced and the task stays where it was

#### a transition the state does not allow comes back as an error, not a mutation

- **Given** a task an agent is working on, which cannot be resumed
- **When** the manager tries to resume it
- **Then** the tool call comes back as an error naming the state
- **Then** the document is untouched, so a refusal changes nothing

#### a task is authored by creating it, writing its body and submitting it

- **Given** a project with no tasks in it yet
- **Given** a task created with a goal and a check, waiting to be submitted
- **When** the manager submits it
- **Then** it enters the pipeline at the design phase

#### a task with dependencies is submitted into BLOCKED

- **Given** a task edited to depend on another that is still open
- **When** the manager submits it
- **Then** it waits rather than being dispatched

#### task_submit refuses a task that depends on itself in a loop

- **Given** two tasks edited to depend on each other
- **When** the manager submits one of them
- **Then** it is refused, naming the cycle, and the task does not move

#### task_submit from MANAGER_REVIEW lands the work and closes the task

- **Given** work that has been reviewed and is waiting on the manager
- **When** the manager accepts it
- **Then** the task closes and its work is on the base branch

#### the views are readable as resources and carry a cursor

- **Given** a project with a task in it
- **Given** another task created beside the first
- **When** the tasks view is read as a resource
- **Then** the tasks view carries rows and a cursor the manager can track
- **Then** the runtime directory is named, so logs can be read from outside

#### disable_agent and enable_agent move every slot of one agent

- **Given** a pool of two agents, one of them with two slots
- **When** the manager disables one of the agents
- **Then** every slot of that agent is disabled, and no other agent's is
- **Then** enabling it again returns every one of its slots to the queue

#### the pool is read from the tasks directory

- **Given** a pool file written into the project's task directory
- **When** the manager reads the slots view
- **Then** the slots come from that file, so the pool travels with the graph

#### task_hold parks a task with the manager's reason on it

- **Given** a task an agent is working on
- **When** the manager parks it
- **Then** it lands in the held state of the phase it was in
- **Then** the reason it was parked is on the document for a person to read

#### task_resume sends a held task back to the phase it was held from

- **Given** a task the manager parked while it was being worked on
- **When** the manager resumes it, having decided the wall is gone
- **Then** it goes back to the phase it was parked from, ready to dispatch

#### task_abort closes a held task for good

- **Given** a task the manager parked and has decided against
- **When** the manager throws it away
- **Then** it is closed, and leaves the pipeline without being worked further

#### enable_scheduler is published where the console reads it

- **Given** a server that starts with nothing being dispatched
- **When** the manager turns dispatching on
- **Then** the next published queue says the scheduler is dispatching

#### disable_scheduler is published where the console reads it

- **Given** a server that has been dispatching
- **When** the manager pauses it
- **Then** the next published queue says the scheduler is paused

#### the error resource is empty while the server is working

- **Given** a server that started cleanly
- **When** the manager reads the error resource
- **Then** there is no error on it

#### slot_abort refuses a slot that is idle

- **Given** a pool whose slots are all sitting idle
- **When** the manager aborts one of them
- **Then** it is refused, because there is no command of its own to kill

#### slot_abort names the pool when the slot is unknown

- **Given** a server whose pool holds one slot
- **When** the manager aborts a slot that does not exist
- **Then** the refusal lists the slots there are, so the name is easy to fix

### Feature: a server that could not start

#### the error resource says why the server did not start

- **Given** a project whose pool file the server refuses to load
- **When** the manager reads the error resource
- **Then** it says the server failed to start, and what the pool file lacks

#### a tool fails with that error rather than killing the server

- **Given** a project whose pool file the server refuses to load
- **When** the manager creates a task
- **Then** the call comes back as an error, and the server is still answering

#### a view resource fails with that error rather than reading nothing

- **Given** a project whose pool file the server refuses to load
- **When** the manager reads the slots view
- **Then** it is refused with why the server did not start

## orchestrator/main/server-checks.test.ts

### Feature: running a task's checks

#### a failing check records the failure and sends the work back

- **Given** a task whose only check writes to stderr and fails
- **When** the work is done and the check runs against it
- **Then** the task goes back to work with the failure queued for the agent

#### every failing check is recorded, not only the first

- **Given** a task with three checks, the first and last of which fail
- **When** the work is done and the checks run against it
- **Then** the agent is told about both failures, not only the first

#### a passing check moves the task to the agent review

- **Given** a task with two checks that both pass
- **When** the work is done and the checks run against it
- **Then** the task moves on to the agent review

#### the whole output of a check is written where a person can read it

- **Given** a task whose check writes to its output
- **Given** a server with its scheduler enabled
- **When** the work is done and the check runs against it
- **Then** the output is on disk in the task's own directory

### Feature: sending a task back to the agent that did it

#### the same session is reopened, the assignment is untouched and the result is cleared

- **Given** a task whose check failed once and whose session was kept
- **When** the agent is dispatched again and finishes the work
- **Then** the assignment was never rotated, because the session was resumed
- **Then** the check failed exactly once, so the second attempt passed

#### the resume prompt carries every failing command into the session

- **Given** a task whose check failed after the first attempt
- **When** the agent is dispatched again into the session it was using
- **Then** its second prompt names the command that failed and how it failed

#### a session opened under an older role directory is reopened where it lies

- **Given** a task whose session was opened where an older server put it
- **Given** a server with its scheduler enabled
- **When** the agent is dispatched again
- **Then** the session is reopened where the document says it lies

### Feature: landing or abandoning finished work

#### merged tears the worktree and the branch down

- **Given** work that has been reviewed and is waiting on the manager
- **When** the manager merges it
- **Then** the task closes, the work is on the base, and nothing is left over

#### abort takes a branch that never landed, and tears it down at CLOSED

- **Given** work that has been reviewed and is waiting on the manager
- **When** the manager aborts it
- **Then** it closes, and the work it did is thrown away with the branch

#### abort takes a task still queued in WORK, via HELD_WORK

- **Given** a task held before any agent ever started it
- **When** the manager aborts it
- **Then** it closes, having left no branch behind at all

#### abort refuses a task an agent is already working on

- **Given** a task an agent is holding and working on
- **When** the manager aborts it
- **Then** it is refused, naming where a task must be to be abandoned

#### abort refuses a branch that already landed

- **Given** work whose branch has already been merged into the base
- **When** the manager aborts it
- **Then** it is refused, because there is no longer work to throw away

#### closing a task deletes its runtime directory

- **Given** work that has been reviewed and is waiting on the manager
- **When** the manager merges it
- **Then** the runtime directory it worked in is removed with it
- **Then** it stays gone once it falls off the end of the recent list

## orchestrator/main/server-dispatch.test.ts

### Feature: where a project's task graph is found

#### with no tasks dir the server resolves ~/task-graph/<key> and seeds the graph

- **Given** a project that has never had a task graph
- **When** a server is started against it with no task directory named
- **Then** it resolves the graph from the project's path and seeds it

#### with no base named the server lands work on the branch the repo is on

- **Given** a project checked out on a branch of its own naming
- **When** a server is started against it without being told a base
- **Then** that branch is the one it will rebase onto and land work on

### Feature: a task that goes all the way through

#### work, checks, agent review and manager review close the task

- **Given** a task with a check, and agents that will do and review the work
- **When** the manager merges it
- **Then** the task closes and nothing of its workspace is left behind

### Feature: measuring how fast an agent writes

#### the output tokens of every message land in the pool's rate table

- **Given** a task whose agents report the tokens each message cost
- **When** the scheduler runs it to the manager
- **Then** the agent that ran has a measured rate and an idle one has none

### Feature: the design and planning phases

#### a plan is written, reviewed and accepted before the work starts

- **Given** a task nobody has designed or planned yet
- **When** the scheduler runs it as far as the work stage
- **Then** the design and the plan are both in the task body
- **Then** it went through each phase and its review in order

#### design review findings go back to the designer, verbatim, until the design passes

- **Given** a design the reviewer will reject once before accepting
- **When** the scheduler runs the task as far as the work stage
- **Then** the designer was told what the reviewer found, in its own words
- **Then** only the design that was accepted is what the task carries

#### design review findings reach the designer without touching the task body

- **Given** a design the reviewer will reject once before accepting
- **When** the scheduler runs the task as far as the plan review
- **Then** the designer read the findings in its next prompt
- **Then** the findings are nowhere in the task body or the assignment

#### a designer that compacts after a rejection is steered with the findings again

- **Given** a designer that compacts on the turn after it was sent back
- **When** the scheduler runs the task as far as the planning stage
- **Then** the steer it was given still carried the findings to answer

#### a designer that keeps submitting an empty design is held

- **Given** a designer that submits without writing anything
- **When** the server nudges it until the attempts run out
- **Then** the task is held, saying what the designer never wrote

#### a design reviewer that writes to the worktree is sent back

- **Given** a design reviewer that leaves a file behind in the worktree
- **When** the scheduler runs the task as far as the work stage
- **Then** it was sent back to clean up before its review was accepted

#### plan review findings go back to the planner, verbatim, until the plan passes

- **Given** a plan the reviewer will reject once before accepting
- **When** the scheduler runs the task as far as the work stage
- **Then** the planner was told what the reviewer found, in its own words
- **Then** only the plan that was accepted is what the task carries

#### an empty plan is asked for again, and a persistent one is held

- **Given** a planner that submits nothing once, then writes its todos
- **When** the scheduler runs the task as far as the work stage
- **Then** it was asked once for the plan and never held

#### a planner that keeps submitting an empty plan is held

- **Given** a planner that submits without writing anything
- **When** the server nudges it until the attempts run out
- **Then** the task is held, saying what the planner never wrote

#### a planner that writes to the worktree is sent back, then held

- **Given** a planner that leaves a file behind in the worktree
- **When** the scheduler runs the task as far as the work stage
- **Then** it was sent back to clean up before its plan was accepted

#### a planner that commits to the branch is sent back, then held

- **Given** a planner that commits work the plan stage may not do
- **When** the server nudges it until the attempts run out
- **Then** the task is held, saying which rule the planner broke

#### a plan reviewer that writes to the worktree is sent back

- **Given** a plan reviewer that leaves a file behind in the worktree
- **When** the scheduler runs the task as far as the work stage
- **Then** it was sent back to clean up before its review was accepted

#### a blocked designer holds the task, and resume sends it back to DESIGN

- **Given** a designer that reports it cannot go on
- **When** the manager resumes it
- **Then** the task starts its design phase over

#### a blocked planner holds the task, and resume sends it back to PLAN

- **Given** a planner that reports it cannot go on
- **When** the manager resumes it
- **Then** the task starts its planning phase over

#### a blocked plan reviewer holds the task, and resume sends it back to PLAN

- **Given** a plan reviewer that reports it cannot go on
- **When** the manager resumes it
- **Then** the task goes back to the planner rather than the reviewer

#### the manager resolves a held task by writing the body and resuming

- **Given** a task held because its goal was unclear to the agent
- **Given** the manager rewrites the goal in the task body
- **When** the manager resumes it
- **Then** the task goes back into the queue with the clearer goal

#### design and planning rank below work, reviews above their fresh states

- **Given** one task in each of the four states the scheduler ranks
- **When** the queue is published
- **Then** work outranks planning, and a review outranks its own fresh stage

#### a task still queued in PLAN can be held and aborted

- **Given** a task queued for design that nothing has started
- **Given** the manager holds it, abandoning it
- **When** the manager aborts it
- **Then** it closes and leaves the graph

### Feature: handing a task to an agent

#### the prompts and templates come from the orchestrator's own directory, not the driven repo

- **Given** a driven project with no orchestrator directory of its own
- **Given** a server with its scheduler enabled
- **When** a task is dispatched
- **Then** the prompts came from the orchestrator's own directory

#### the driven repo's own orchestrator/ overrides the prompts and templates it names

- **Given** a project that carries its own copy of the work prompt
- **Given** a server with its scheduler enabled
- **When** a task is dispatched
- **Then** the agent read the project's words, over the task's own body

#### startup logs the absolute path of every cached prompt and template

- **Given** a project that overrides one of the prompts
- **When** the server starts against it
- **Then** the override is named in the log, and the file it replaced is not

#### an override of one file leaves every other prompt as the orchestrator ships it

- **Given** a project that overrides the design prompt but not the work one
- **Given** a server with its scheduler enabled
- **When** a task is dispatched into the work stage
- **Then** the agent read the orchestrator's own work prompt, untouched

#### a dispatched task gets a worktree, a branch and an ASSIGNMENT.md beside it

- **Given** a task with a goal written into its body
- **Given** a server with its scheduler enabled
- **When** the task is dispatched
- **Then** it has a worktree, a branch, and its assignment beside the worktree

#### the assignment is handed over with the empty section the agent is to write

- **Given** a task about to be dispatched into the design stage
- **Given** a server with its scheduler enabled
- **When** the task is dispatched
- **Then** the assignment is the body plus the empty heading to fill in

#### the claim records the agent, its pid and the session it opened

- **Given** a task about to be dispatched
- **Given** a server with its scheduler enabled
- **When** the agent claims it
- **Then** the document names the agent, its process and everything it needs
- **Then** the claim is released once the work is done

#### a workspace claimed under an older branch prefix keeps the branch it recorded

- **Given** work committed on a branch named by an older version of the server
- **When** the task is reviewed and merged under the current server
- **Then** the branch it recorded is the one used, not the one it would pick

#### nothing is dispatched while the scheduler is stopped

- **Given** a queued task and a server whose scheduler is stopped
- **When** the server ticks twice
- **Then** the task is still queued and nothing was started

#### a paused scheduler still settles work already in flight

- **Given** a task already dispatched to an agent
- **When** the scheduler is stopped while the agent is still working
- **Then** its submit is still applied and its checks still run

### Feature: landing work on the base branch

#### a branch that no longer rebases is an error back to the manager

- **Given** finished work whose base has since changed the same file
- **When** the manager merges it
- **Then** the manager is told it no longer rebases, and the task is untouched

#### a check that fails after the rebase is an error back to the manager

- **Given** finished work whose branch no longer passes its own check
- **When** the manager merges it
- **Then** the check that failed is named, and the task is untouched

#### the repo the work came from is read-only to everything the server spawns

- **Given** a task whose check tries to write into the project it came from
- **When** the work is done and the check runs against it
- **Then** the write is refused, and the project is untouched

## orchestrator/main/server-recovery.test.ts

### Feature: picking a project back up at startup

#### a graph another server holds keeps a new server out

- **Given** a task graph a live server elsewhere already holds
- **When** a server starts against that graph
- **Then** it is refused, and leaves no runtime lock behind to wedge a retry

#### a server gives the graph back when it shuts down

- **Given** a running server holding the graph it writes
- **When** the server shuts down
- **Then** the graph is free again for the next server to take

#### a worktree lost to a cleared /tmp is recreated from its branch

- **Given** a task whose worktree was cloned and then removed from disk
- **When** a new server starts against the same project
- **Then** the worktree is cloned again from the branch that survived

#### a second server against a live one refuses to start

- **Given** a server already running against a project
- **When** another server starts against the same project
- **Then** it refuses, naming the server that holds the runtime directory

#### a second server continues the transition log rather than restarting it

- **Given** a server that has applied a transition and then exited
- **When** a new server starts against the same project
- **Then** it carries on the sequence, so the manager's cursor still means something

#### a directory that is not a git repository is refused at startup

- **Given** a project directory that is not under version control
- **When** a server is started against it
- **Then** it refuses at startup rather than on the first dispatch

### Feature: reaping a claim whose agent is gone

#### a claim whose process is gone is cleared

- **Given** a task claimed by an agent whose process has since exited
- **When** the server ticks over the graph
- **Then** the claim is dropped, the stage is kept and the workspace survives

#### an agent that dies mid-task frees its slot and releases the task

- **Given** a task dispatched to an agent that will exit without settling
- **Given** a server with its scheduler stopped, so the reaper must find it
- **When** the agent dies and the server ticks on
- **Then** the slot is idle and the task is back in the queue where it stood

#### a slot whose process died no longer shields the task from the reaper

- **Given** a task dispatched to an agent that will exit without settling
- **When** the reaper runs over the graph
- **Then** the dead slot does not shield the task, and is released with it

#### a check running for a merge is left alone by the reaper

- **Given** a merge whose re-run checks are still going
- **When** the server ticks while the check is running
- **Then** the task is left where it is, and the merge runs to completion

#### a live claim is left alone

- **Given** a task claimed by a process that is still running
- **When** the server ticks over the graph
- **Then** the claim is left alone, because the agent is still working

### Feature: an abort that races a dispatch

#### a task aborted while its agent is spawning is closed and never claimed

- **Given** a dispatch that is part way through opening its session
- **When** the manager holds and aborts the task before the claim lands
- **Then** the task is closed, and the dispatch never claimed it

#### the slot a lost dispatch was using is released, not stranded

- **Given** a dispatch that is part way through opening its session
- **When** the manager holds and aborts the task before the claim lands
- **Then** the slot goes back to idle, and the lost dispatch is logged

#### a dispatch that wins the race claims the task as normal

- **Given** a task the scheduler is about to dispatch
- **When** the dispatch completes before anything aborts it
- **Then** the agent holds the task, and the manager can no longer abort it

### Feature: a task document that does not parse

#### one unreadable task does not stop the others being dispatched

- **Given** a graph with one unreadable task and one good one
- **When** the scheduler runs over the graph
- **Then** the good task is worked to completion and the broken one ignored

#### one unreadable task does not stop the reaper

- **Given** a graph with one unreadable task and one dead claim
- **When** the server ticks over the graph
- **Then** the reaper still runs over the tasks it could read

#### the file is logged once when it breaks and once when it is repaired

- **Given** a server that has ticked five times over an unreadable task
- **Given** the document is repaired
- **When** the server ticks five times more
- **Then** the repair is logged once, and the task is back in the graph

### Feature: an agent that compacts mid-turn

#### a designer's scribbles are thrown away and the dispatch is steered back

- **Given** a designer that scribbles in its worktree and then compacts
- **When** it runs to its submit
- **Then** the worktree is reset and the agent steered back to its assignment

#### a worker keeps its worktree and is steered back to the assignment

- **Given** a worker that commits its work and then compacts
- **When** it runs to its submit
- **Then** its work is kept, and it is only steered back to the assignment

#### a reviewer that compacts right after submitting keeps its result

- **Given** a reviewer that compacts after it has already called its result
- **When** it runs to its submit
- **Then** it is left alone to settle, rather than steered off its result

### Feature: a dependency that can never be satisfied

#### a task that can never unblock is logged once, not every tick

- **Given** two tasks that depend on each other
- **When** the server ticks five times over them
- **Then** each is reported once, rather than on every tick forever

## orchestrator/main/server-settle.test.ts

### Feature: what a reviewer sends back to the worker

#### a finding lands under # Review findings and the task drops back to WORK

- **Given** work that a reviewer will find a problem with
- **When** the work is done, checked and reviewed
- **Then** the finding is written into the task body for the worker to read
- **Then** it is also left where the next dispatch will pick it up
- **Then** the transition is recorded as the server's, not the reviewer's

#### the findings reach the worker as its next prompt, and are cleared once it submits

- **Given** work sent back once by a review and then finished
- **When** the task runs to the manager
- **Then** the worker's second prompt carried the findings it had to answer
- **Then** nothing is left queued once the worker has answered them

#### the worker sent back by a review works in its own session, never the review's

- **Given** work sent back once by a review and then finished
- **When** the task runs to the manager
- **Then** the worker was resumed in its own session, twice, never the review's

#### the reviewer gets its own session and a worktree with the work on it

- **Given** work an agent committed and a reviewer accepted
- **When** the task runs to the manager
- **Then** the reviewer saw a worktree with the work committed on it
- **Then** the reviewer read the work in a session of its own

### Feature: a review that fails twice

#### a first review failure bounces the work back and is counted

- **Given** work a reviewer will send back
- **Given** a server with its scheduler enabled
- **When** the work is done, checked and reviewed once
- **Then** the failure is counted and the work only bounced back

#### a second review failure holds the task with the findings as the reason

- **Given** work sent back once by a review
- **When** the review fails a second time
- **Then** the task is held with the second round's findings as the reason
- **Then** only the first round's findings reached the body
- **Then** the count file is gone and the findings wait for the resume

#### a review that finally passes clears the count

- **Given** work sent back once and then accepted
- **When** the task runs to the manager
- **Then** the passing review cleared the count

#### manager review bounces are never counted

- **Given** work the manager will send back twice
- **When** the manager sends it back again after the redo
- **Then** it is back in WORK, never held, and no review was ever counted

#### resume after a review-failure hold starts the count fresh

- **Given** work held after two review failures
- **Given** the manager resumes it, and a server with its scheduler enabled
- **When** the redo fails once
- **Then** one failure only bounces it, never holds it again

### Feature: a submit with nothing committed behind it

#### a branch with no commit on it comes back for one

- **Given** a worker that submits once without committing, then commits
- **When** the task runs to the manager
- **Then** it was nudged once and never held, because the second try passed

#### an uncommitted change comes back with what git status reports

- **Given** a worker that leaves one file uncommitted, then commits it
- **When** the task runs to the manager
- **Then** it was told which file it had left behind, and committed it

#### an agent that never commits is held, and the slot is released

- **Given** a worker that submits without ever committing anything
- **When** the server nudges it until the attempts run out
- **Then** the task is held with a reason the manager can act on

### Feature: an agent that stops short of finishing

#### a blocked result holds the task with its message as the reason

- **Given** a worker that reports it cannot go on
- **When** the server settles its turn
- **Then** the task is parked with the agent's own words as the reason

#### an agent stuck on one command is asked whether it is blocked, not held

- **Given** a worker that repeats one command and then does the work
- **When** the task runs to the manager
- **Then** it was asked about the command it repeated, and never held

#### an agent that keeps looping is held only once the nudges run out

- **Given** a worker that repeats one command every time it is nudged
- **When** the server nudges it until the attempts run out
- **Then** the task is held, naming the command it was stuck on

#### a reviewer that reconsiders its blocker submits instead

- **Given** a reviewer that reports a blocker and then submits anyway
- **When** the task runs to the manager
- **Then** it was asked once and the task was never held

#### a held task is never dispatched again

- **Given** a task held after its agent reported a blocker
- **When** the scheduler runs for three more ticks
- **Then** it is still held, and no second agent was ever prompted

#### a nudge waits for the turn the agent is already in

- **Given** an agent that answers in prose and takes time to start each turn
- **When** the server nudges it until the attempts run out
- **Then** no nudge was sent into a turn the agent was still inside

#### an edited assignment is restored above the notes, and the redo passes

- **Given** a worker that rewrites the part of its assignment it may not
- **When** the task runs to the manager
- **Then** what it changed was put back and only its own section survives

#### a worker that submits without appending notes is prompted, then held

- **Given** a worker that commits but never writes its notes
- **When** the server nudges it until the attempts run out
- **Then** the task is held, saying which part of the assignment is missing

### Feature: a review that comes back unusable

#### an assignment the reviewer changed is restored, and the review is prompted in place

- **Given** a reviewer that rewrites the assignment it was given
- **When** the task runs to the manager
- **Then** the task never went back to the worker over the reviewer's mistake
- **Then** the reviewer was prompted again in place, and its edit was undone

### Feature: keeping the attempts an agent already made

#### a re-dispatch rotates the previous attempt into history

- **Given** a task held after an attempt that wrote notes into its assignment
- **Given** the manager resumes it with a plan that lets the redo finish
- **When** the second attempt finishes the work
- **Then** the first attempt is kept in history and the second is the one that counts

### Feature: a failure while finishing with an agent

#### the slot is freed and the manager survives instead of rejecting into nothing

- **Given** an agent whose abort leaves the workspace unable to be harvested
- **When** the server settles its turn and finishes with it
- **Then** the slot is freed and the failure is logged rather than thrown away

## orchestrator/main/server-views.test.ts

### Feature: the views the console and the manager read

#### an idle slot is a row with no task in it, never a missing row

- **Given** a pool of two slots the scheduler has dispatched nothing to
- **When** the views are published
- **Then** every slot is a row, so the console shows the whole pool

#### a busy slot names its task, role, pid and activity

- **Given** a task the scheduler is about to dispatch
- **Given** a server with its scheduler enabled
- **When** the agent is dispatched and the views are published
- **Then** its row names the task, the role, the process and what it is doing

#### every view carries the same transition cursor

- **Given** a server with a task in its graph
- **When** the views are published
- **Then** every one of them is stamped with the same cursor

#### the tasks view carries the blocking count and the held reason

- **Given** a task another task waits on, held on something only a person can fix
- **When** the views are published
- **Then** its row says how much it blocks and what it is waiting on

### Feature: the queue view

#### the queue is what the scheduler would dispatch next, with its own state

- **Given** one queued task and one blocked behind it
- **When** the views are published with the scheduler paused
- **Then** the queue holds only what could be dispatched, with its own rank
- **Then** the switch in the view follows the scheduler it draws

### Feature: a pool with no agents in it

#### starting the scheduler is refused, naming the file to add an agent to

- **Given** a server whose pool file declares no agents
- **When** the scheduler is started
- **Then** it is refused, naming the pool file a person can add an agent to

#### a task authored with no agents still reaches the queue

- **Given** a server whose pool file declares no agents
- **When** the views are published
- **Then** the task is queued, waiting for an agent to be added

#### the slots view names the pool file the console tells a person to edit

- **Given** a server whose pool file declares no agents
- **When** the views are published
- **Then** the slots view carries the path of that file

### Feature: what the slots view says about a running agent

#### tokens, context and the session file reach the view

- **Given** a task the scheduler is about to dispatch
- **Given** a server with its scheduler enabled
- **When** the agent is dispatched and the views are published
- **Then** the console can show how much context is left and where to read it

#### compactions are counted in the view

- **Given** an agent that will compact part way through its turn
- **Given** a server with its scheduler enabled
- **When** it is dispatched and runs until it compacts
- **Then** the console can see how often it has compacted on this task

#### a closed task stays in the tasks view

- **Given** work that has been reviewed and is waiting on the manager
- **When** the manager merges it
- **Then** the closed task is still shown, so the manager sees what just landed

### Feature: the log of every transition applied

#### every applied transition is one line with a from, a to and an author

- **Given** a task worked on, checked and reviewed
- **When** it runs to the manager
- **Then** every transition is one numbered line, in the order it happened
- **Then** each line says where the task came from, went to, and who moved it

### Feature: commands the console writes for the server

#### a written command toggles the scheduler and an agent, and is consumed

- **Given** a running server watching for console commands
- **When** the console writes the first of the three commands
- **Then** each is applied and the file is consumed rather than reapplied

#### a command left behind by a dead server is applied at startup

- **Given** a command written while no server was listening
- **When** a new server starts against the same project
- **Then** it applies the waiting command and consumes it

#### a command naming no agent in the pool is logged, not thrown

- **Given** a running server watching for console commands
- **When** the console names an agent that is not in the pool
- **Then** the refusal is logged and the server carries on running

### Feature: turning an agent off and on

#### an agent configured disabled is never dispatched to

- **Given** a queued task and a pool whose only agent is turned off
- **Given** a server with its scheduler enabled
- **When** the scheduler runs over the graph
- **Then** nothing is dispatched, and the console says why

#### disabling an agent disables every one of its slots

- **Given** a pool of two agents, one of them with three slots
- **When** one of them is disabled
- **Then** every slot of that agent is disabled, and the other agent is not

#### a disabled agent takes no work and a re-enabled one takes it again

- **Given** a queued task and an agent that has been disabled
- **When** the agent is enabled again
- **Then** the task it could not take is dispatched and worked to the manager

#### a slot running when its agent is disabled finishes that task first

- **Given** an agent part way through a task
- **When** its agent is disabled
- **Then** the slot keeps its task, showing as running under a disabled agent
- **Then** it finishes the work before the slot is taken out of the pool

#### a slot name passed where an agent belongs is refused

- **Given** a server whose pool holds the one agent pi-fake-fake
- **When** the name of that agent's first slot is passed where an agent belongs
- **Then** it is refused as no agent of that name

#### a name the pool never held is refused with the agents it does hold

- **Given** a server whose pool holds the one agent pi-fake-fake
- **When** the name nope is passed where an agent belongs
- **Then** it is refused, and the pool it does have is named

### Feature: aborting the command an agent is running

#### aborting a busy slot kills the bash call and the agent finishes its turn

- **Given** an agent stuck inside a long-running command
- **When** the manager aborts that command
- **Then** the command is killed and the agent finishes its turn from there

#### aborting an IDLE slot throws

- **Given** a slot the scheduler has dispatched nothing to
- **When** the manager aborts it
- **Then** it is refused, because there is no command to kill

#### aborting an unknown slot name throws, listing the pool's slot names

- **Given** a server whose pool holds one slot
- **When** the manager aborts a slot that is not in the pool
- **Then** it is refused, and the slot names it does have are listed

#### aborting by agent key (no slot suffix) throws

- **Given** a server whose slot names carry a number
- **When** the manager aborts using the agent key rather than the slot name
- **Then** it is refused, because an abort names one running process

#### a written slot_abort command file drives applyCommand and kills the tool call

- **Given** an agent stuck inside a long-running command
- **When** the console writes an abort for that slot
- **Then** the command is killed, exactly as the manager's own abort would

#### a written slot_abort naming an idle slot is logged and dropped

- **Given** a slot the scheduler has dispatched nothing to
- **When** the console writes an abort for that slot
- **Then** the refusal is logged and the server carries on running

### Feature: a manager that exits while its agents run on

#### detaching stops the server listening on the console channel

- **Given** a running server that has applied a console command already
- **When** the manager detaches from it
- **Then** a command written afterwards is neither consumed nor applied
- **Then** the views it published are left on disk for the next manager

#### a slot still running for a previous manager is not offered as capacity

- **Given** a published view naming a slot whose process is still running
- **Given** a server with its scheduler enabled
- **When** a new server starts and ticks with the scheduler on
- **Then** the slot is reattached rather than dispatched to again

## orchestrator/policy/console.test.ts

### Feature: joining a slot to the task it is running

#### an agent is shown with its task and the check running against it

- **Given** a view where a check is running on the task the agent holds
- **When** the view is joined into one pane per slot
- **Then** the pane carries the task's state and the check's command
- **Then** the detail line names the task, the role and the process
- **Then** the running check is what the pane says the agent is doing

#### a disabled slot's pane is drawn to the right of a running one

- **Given** a view whose first slot is disabled and whose second is running
- **When** the view is joined into one pane per slot
- **Then** the running slot comes first, so the disabled ones group at the right

#### an idle slot shows no task, no check and no clock

- **Given** a view whose only slot is idle
- **When** the view is joined into one pane per slot
- **Then** the pane has nothing to draw but the slot itself

#### a task the view has dropped still draws from the agent row

- **Given** a view whose task list no longer carries the task an agent holds
- **When** the view is joined into one pane per slot
- **Then** the pane still names the task and the role, without its state

#### a slot waiting to retry shows when it will and which attempt

- **Given** a slot backing off after a provider error
- **When** the pane's detail line is drawn
- **Then** it says which attempt is next and at what time

### Feature: the numbers on a pane header

#### the stats line shows the token rate and the context left

- **Given** a busy pane whose agent has used two-fifths of its context
- **When** the stats line is drawn with a measured rate
- **Then** it reads as the rate and the context, both rounded for a terminal

#### an agent that has compacted has the count after its context

- **Given** a pane whose agent has compacted three times on this task
- **When** the stats line is drawn
- **Then** the compaction count follows the context percentage

#### a duration of seconds reads as whole seconds

- **Given** a slot that has been running for five and a half seconds
- **When** it is written for a header
- **Then** it reads as five seconds, the largest unit that fits it

#### a duration past a minute reads as minutes and seconds

- **Given** a slot that has been running for ninety-five seconds
- **When** it is written for a header
- **Then** it reads as one minute and thirty-five seconds

#### a duration past an hour reads as hours and minutes

- **Given** a slot that has been running for an hour and three minutes
- **When** it is written for a header
- **Then** it reads as one hour and three minutes, dropping the seconds

#### a duration that ran backwards reads as no time at all

- **Given** a duration measured across a clock that went backwards
- **When** it is written for a header
- **Then** it reads as zero, never as a negative time

#### a token count below a thousand is written out in full

- **Given** a count of tokens below a thousand
- **When** it is written for a header
- **Then** every digit of it is shown

#### a token count past a thousand is abbreviated

- **Given** a count of tokens past a thousand
- **When** it is written for a header
- **Then** it is written in thousands, to one decimal place

### Feature: which lines of a transcript a pane shows

#### a following pane shows the newest lines when they overflow it

- **Given** three lines of transcript and a pane two rows tall
- **When** the pane is drawn while following the bottom
- **Then** it shows the newest lines and drops the oldest

#### scrolling back by a line reveals the line above

- **Given** three lines of transcript and a pane one row tall
- **When** the pane is drawn one line back from the bottom
- **Then** the line above the newest is what is shown

#### scrolling back further than there is history stops at the top

- **Given** three lines of transcript and a pane one row tall
- **When** the pane is drawn far further back than it has history
- **Then** it shows the first line rather than scrolling past it

#### a frozen anchor past the end of a shrunken pane is pulled back

- **Given** a pane frozen at a line the transcript no longer reaches
- **When** the anchor is worked out for a pane with one line in it
- **Then** it is pulled back to the top, which is as far as the pane goes

#### lines arriving below a scrolled pane do not move what it shows

- **Given** a scrolled pane showing a fixed part of its transcript
- **Given** two more lines arriving under it
- **When** the pane is shown again
- **Then** the reader is still looking at the same lines

#### lines arriving under a following pane do move it

- **Given** a pane following the bottom of its transcript
- **Given** another line arriving under the pane
- **When** the cache is updated
- **Then** the pane moves down to it

#### panes of different lengths scroll by the same number of lines

- **Given** one short transcript and one long one, in panes of the same height
- **When** both are scrolled back by two lines
- **Then** each has moved two lines from its own bottom, not to a shared line

#### a pane that runs out of history waits at its top for the others

- **Given** a transcript shorter than the pane and one longer than it
- **When** both are scrolled back by one line
- **Then** the short one stays at its first line while the long one keeps going

#### the body leaves room for the queue line, the header and its rule

- **Given** a terminal twenty-four rows tall
- **When** the height left for the transcript is worked out
- **Then** the rows the queue and the header take are all subtracted

#### a usage record is not drawn in the transcript

- **Given** a transcript whose only entry is a usage record
- **When** the pane's lines are built
- **Then** nothing is drawn, because usage belongs on the header

#### a wide pane keeps the timestamp and label on the first line

- **Given** an entry that fits comfortably in its pane
- **When** the entry is laid out for a forty-column pane
- **Then** it is one line, led by the time and who said it

#### a pane too narrow for the prefix puts the text on its own lines

- **Given** an entry whose prefix alone would fill the pane
- **When** the entry is laid out for a fourteen-column pane
- **Then** the prefix takes a line of its own and the text follows below it

### Feature: scrolling every pane together

#### the first step back freezes where every pane's bottom was

- **Given** every pane following the bottom of its own transcript
- **When** the reader steps back one line
- **Then** each pane's bottom is pinned, and both move by the same one line

#### later steps back keep the bottoms that were already frozen

- **Given** panes already scrolled back, with their bottoms pinned
- **When** the reader steps back two more lines, after new lines arrived
- **Then** the pinned bottoms are kept, so the new lines do not shift the view

#### a pane that runs out of history stops without holding the others

- **Given** one pane with four lines of history and one with nine
- **When** the reader steps back six lines
- **Then** the short pane stops at its top while the long one goes the full six

#### a pane at its top moves forward with the rest

- **Given** both panes scrolled to the top of their history
- **When** the reader moves forward two lines
- **Then** both move together, rather than the short one waiting

#### moving forward while already following does nothing

- **Given** every pane following the bottom of its own transcript
- **When** the reader moves forward
- **Then** nothing changes, because there is nothing below the bottom

#### every pane reaching its bottom puts the console back into following

- **Given** panes scrolled back far enough that one is two lines from its bottom
- **When** the reader moves forward past the bottom of both
- **Then** the frozen bottoms are dropped and the console follows again

#### the top key takes every pane to its first line

- **Given** every pane following the bottom of its own transcript
- **When** the reader jumps to the top
- **Then** each pane is scrolled back by the whole of its own history

#### the bottom key puts the console back into following

- **Given** panes scrolled back from their bottoms
- **When** the reader jumps to the bottom
- **Then** the frozen bottoms are dropped and the console follows again

#### the new messages button sits centred one row above the bottom

- **Given** a terminal a hundred columns wide and twelve rows tall
- **When** the button's place on the screen is worked out
- **Then** it is one row up from the bottom and centred across the terminal

#### the button is laid over a row without moving its columns

- **Given** a full-width row of a pane, and where the button belongs
- **When** the button is laid over that row
- **Then** the row is still exactly as wide, with the button in its columns

#### a click lands on the button only inside the columns it covers

- **Given** the button drawn on a hundred-column terminal
- **When** clicks are tried at its edges and one row above it
- **Then** only the click inside its own row and columns counts

### Feature: keeping a pane's wrapped lines between frames

#### lines already wrapped are kept rather than wrapped again

- **Given** a pane whose transcript has already been laid out
- **Given** another entry arriving below it
- **When** the pane is laid out again
- **Then** the settled lines are the same objects, not rebuilt every frame

#### the newest entry is laid out again, because it is still growing

- **Given** a pane whose last entry is an answer still being streamed
- **When** more text arrives on that last entry
- **Then** the pane redraws it rather than showing the half of it it cached

#### a resized terminal rebuilds every line of the pane

- **Given** a pane laid out for a narrow terminal
- **When** the terminal is widened and the pane laid out again
- **Then** the lines are rewrapped to the new width and cached at it

#### a session rewritten from the start drops the lines it cached

- **Given** a pane holding the lines of a session that has since been replaced
- **Given** a transcript that has come back shorter than it was
- **When** the pane is updated to the shorter transcript
- **Then** the old lines are thrown away and only the new session is drawn

### Feature: drawing the whole screen

#### a disabled pane carries a button to hide every disabled agent

- **Given** one running slot and one whose agent has been turned off
- **When** the screen is drawn
- **Then** the button sits in the middle of the disabled pane, and is a target

#### two disabled panes share one button, centred across both

- **Given** one running slot and two whose agents have been turned off
- **When** the screen is drawn
- **Then** one button is drawn, in the middle of the two disabled panes

#### a screen with nothing running offers no button to hide with

- **Given** two slots whose agents have both been turned off
- **When** the screen is drawn
- **Then** nothing offers to hide, because hiding them would leave an empty screen

#### a running pane carries no button to hide anything

- **Given** two slots that are both running
- **When** the screen is drawn
- **Then** nothing offers to hide, because there is nothing disabled to hide

#### hidden agents collapse to one column at the right of the screen

- **Given** one running slot, and two disabled agents hidden away
- **When** the screen is drawn with them hidden
- **Then** the column is eight columns wide, and the pane takes the rest

#### the collapsed column is flush right whatever the panes leave over

- **Given** three running slots, whose widths do not divide the terminal evenly
- **When** the screen is drawn with two agents hidden away
- **Then** the column still ends at the last column, and its targets sit on it

#### the collapsed column says how to show the agents again

- **Given** one running slot, and two disabled agents hidden away
- **When** the screen is drawn with them hidden
- **Then** the button reads down three lines, each of them a target

#### a console that cannot draw says why in the middle of the screen

- **Given** a failure that stopped the frame being drawn at all
- **When** the screen is drawn from that failure
- **Then** the message is centred, under a line saying the console cannot draw

#### a console that cannot draw offers nothing to click on

- **Given** a failure that stopped the frame being drawn at all
- **When** the screen is drawn from that failure
- **Then** there is no switch and nothing to scroll, because there is no pane

#### a pool with no agents says which file to add one to

- **Given** a console whose server has no agent slots to draw
- **When** the screen is drawn
- **Then** the queue stays on top and the file to edit is centred below it

#### a pool with no agents offers no pane to click on

- **Given** a console whose server has no agent slots to draw
- **When** the screen is drawn
- **Then** there is no switch and nothing to scroll, because there is no pane

#### the screen is exactly one line per row of the terminal

- **Given** two panes on a terminal twelve rows tall
- **When** the screen is drawn
- **Then** it fills the terminal exactly, without scrolling it

#### the queue header is the first row, above every pane

- **Given** two panes and a queue line to draw above them
- **When** the screen is drawn
- **Then** the queue is the top row and the panes start below it

#### a rule under the queue splits it from the panes

- **Given** two panes and a queue line to draw above them
- **When** the screen is drawn
- **Then** the second row is a full-width rule, forked where the panes divide

#### the divider between panes crosses where the header ends

- **Given** two panes side by side
- **When** the screen is drawn
- **Then** the divider is a plain bar until it crosses the header's rule

#### emoji in a transcript keep the divider in the same column

- **Given** two screens differing only in the emoji one transcript carries
- **When** the emoji screen is drawn
- **Then** the divider stands in the same column on every row of both

#### lines arriving below a scrolled screen raise the new messages button

- **Given** a screen scrolled back with more transcript below it
- **When** the screen is drawn
- **Then** the button appears, so the reader knows there is more to see

#### a screen that is following never shows the button

- **Given** a screen following the bottom of a long transcript
- **When** the screen is drawn
- **Then** no button appears, because the reader is already at the newest line

#### a scrolled screen with nothing new below it shows no button

- **Given** a screen frozen at the bottom its transcript already reached
- **When** the screen is drawn
- **Then** no button appears, because nothing has arrived since it froze

#### lines arriving do not move what a scrolled screen is showing

- **Given** a scrolled screen and the same screen with two more lines in it
- **When** both of those screens are drawn
- **Then** the reader is looking at exactly the same transcript

#### the screen reports each pane's bottom, which bounds the scroll

- **Given** two panes whose transcripts are shorter than the pane
- **When** the screen is drawn
- **Then** each bottom is the top of the pane, so there is nothing to scroll to

#### every pane's switch is a click target on its header row

- **Given** two panes side by side
- **When** the screen is drawn
- **Then** each pane offers a switch, on its header row and at its own column
- **Then** clicking one asks for the state its agent is not in

#### a pane inside a bash call offers an abort target on its activity row

- **Given** one pane whose agent is inside a bash tool call
- **When** the screen is drawn
- **Then** the abort target sits on the activity row and names that slot

#### each pane's abort target sits in that pane's own columns

- **Given** two panes side by side, both inside a bash call
- **When** the screen is drawn
- **Then** each abort target falls within the columns of its own pane

#### a click inside the abort target sends the abort, and past it sends nothing

- **Given** a screen with one abort target on it
- **When** a click lands on its first column and another one past its last
- **Then** only the click inside the target sends the command

#### an idle pane offers no abort target

- **Given** a pane whose slot is idle
- **When** the screen is drawn
- **Then** there is no abort target, because there is no command to kill

#### a terminal too narrow for its panes says how many columns it needs

- **Given** two panes and a terminal wide enough for barely one
- **When** the screen is drawn
- **Then** it refuses, rather than drawing something unreadable

#### a screen with no slots at all refuses to draw

- **Given** a view whose agents list is empty
- **When** the screen is drawn
- **Then** it refuses, because an empty pool means the server never loaded

#### a single pane takes the whole terminal

- **Given** a terminal a hundred columns wide, split one way
- **When** the pane's width is worked out
- **Then** no dividers are needed and the pane takes every column

#### two panes each leave one column for a divider

- **Given** a terminal a hundred columns wide, split two ways
- **When** the pane's width is worked out
- **Then** the divider's column is subtracted before the split

#### three panes leave two columns for the dividers between them

- **Given** a terminal a hundred columns wide, split three ways
- **When** the pane's width is worked out
- **Then** both dividers' columns are subtracted before the split

### Feature: the switches on a pane header

#### a switch that is on shows its knob on the right

- **Given** a switch that is on, with no label
- **When** it is drawn for a console that can click it
- **Then** the knob sits on the on side

#### a switch that is off shows its knob on the left

- **Given** a switch that is off, with no label
- **When** it is drawn for a console that can click it
- **Then** the knob sits on the off side

#### a labeled switch carries its label after the knob

- **Given** a switch that is on, labeled scheduler
- **When** it is drawn for a console that can click it
- **Then** the label follows the knob on the same line

#### the pane header leads with the agent's switch

- **Given** a busy pane in a sixty-column terminal
- **When** its header is drawn
- **Then** the switch comes first, then the identity, then how long it has run

#### a narrow pane clips the identity rather than the switch or the state

- **Given** a busy pane in a thirty-column terminal
- **When** its header is drawn
- **Then** the identity is what gives way, and the row still fills the pane

#### a disabled slot reads as idle behind an off switch

- **Given** a slot whose agent has been turned off
- **When** its header is drawn
- **Then** the switch says disabled and the slot itself still reads as idle

### Feature: the abort button on a pane

#### a slot doing nothing offers no button

- **Given** a slot the scheduler has dispatched nothing to
- **When** the button is drawn
- **Then** there is none, because there is no command to kill

#### a slot inside a bash call offers a button

- **Given** a slot inside a bash call
- **When** the button is drawn
- **Then** it offers to abort, because a running command can be killed

#### a slot that is thinking offers no button

- **Given** a slot that is thinking rather than inside a bash call
- **When** the button is drawn
- **Then** there is none, because only a command can be killed

#### a slot that is compacting offers no button

- **Given** a slot compacting an overflowing context rather than inside a bash call
- **When** the button is drawn
- **Then** there is none, because only a command can be killed

#### a slot inside a read call offers no button

- **Given** a slot reading a file rather than inside a bash call
- **When** the button is drawn
- **Then** there is none, because only a command can be killed

#### the button sits at the right of the activity row

- **Given** a slot inside a bash call
- **When** the header is drawn
- **Then** the activity is at the left of the row and the button at its right

#### an idle pane's activity row is blank

- **Given** a slot the scheduler has dispatched nothing to
- **When** the header is drawn
- **Then** the activity row carries nothing at all

#### a long command is clipped rather than running under the button

- **Given** a slot running a command far wider than its pane
- **When** the header is drawn for a thirty-column pane
- **Then** the command is clipped, the elapsed time and button both survive

### Feature: the queue line above the panes

#### the scheduler switch is the leftmost thing on the line

- **Given** a view with the scheduler running and one task queued
- **When** the queue line is drawn
- **Then** the switch comes before the first queued task

#### the queue reads left to right and ends with how many are waiting

- **Given** a view with three tasks queued at different ranks
- **When** the queue line is drawn
- **Then** the tasks read in dispatch order, each with its rank
- **Then** the total sits at the right, and the line fills the terminal

#### a queued task is labelled with the state it will be dispatched into

- **Given** a view with one fresh task queued for work
- **When** the queue line is drawn
- **Then** the task is labelled with its state rather than its rank's name

#### an empty queue says so rather than showing a zero

- **Given** a view with nothing queued
- **When** the queue line is drawn
- **Then** it reads as nothing queued

#### a queue too long for the terminal keeps the count and drops tasks

- **Given** a view with forty tasks queued, on a sixty-column terminal
- **When** the queue line is drawn
- **Then** the total survives, the line fits, and the tail of the queue is cut

#### the scheduler switch asks for the state it is not in

- **Given** a view with the scheduler running
- **When** the queue line is drawn
- **Then** its switch is a target at the top left that would turn it off

## orchestrator/policy/keys.test.ts

### Feature: reading a chunk of terminal input

#### a burst of wheel events is read as every event in it

- **Given** three wheel reports that arrived in one read
- **When** the chunk is split into keys
- **Then** each scroll counts, rather than the burst counting once

#### arrow keys held down are read as one key each

- **Given** a chunk of arrow key presses from a held key
- **When** the chunk is split into keys
- **Then** each press moves the scroll by one, as it would typed slowly

#### ordinary characters are one key each

- **Given** a chunk of plain keys typed quickly
- **When** the chunk is split into keys
- **Then** each character is its own key

#### page keys keep the tilde that ends them

- **Given** page down followed by page up
- **When** the chunk is split into keys
- **Then** each sequence is kept whole, tilde and all

### Feature: where the mouse was clicked

#### a press reports its button and the cell it landed in

- **Given** an sgr mouse report of a press in the terminal's twelfth column
- **When** it is read as a mouse event
- **Then** the cell is zero-based, so it lines up with what was drawn

#### a release is told apart from a press

- **Given** an sgr mouse report ending in a lowercase m
- **When** it is read as a mouse event
- **Then** it reads as a release rather than a press

#### a key that is not a mouse report is not one

- **Given** an arrow key rather than a mouse report
- **When** it is read as a mouse event
- **Then** nothing comes back, and the key is handled as a key

#### a click inside a switch sends the command that switch carries

- **Given** the scheduler switch occupying the first four columns of the top row
- **When** a click lands inside it
- **Then** the switch's command is the one that goes to the server

#### a click past the end of a switch sends nothing

- **Given** the scheduler switch occupying the first four columns of the top row
- **When** a click lands one column past it
- **Then** nothing is sent, because the click hit no target

#### a click on the row below a switch sends nothing

- **Given** the scheduler switch occupying the first four columns of the top row
- **When** a click lands in the same columns one row down
- **Then** it is outside the switch, because a target is one row tall

## orchestrator/policy/scheduler.test.ts

### Feature: which task is dispatched next

#### a task is counted as blocking everything that waits on it, however far

- **Given** a chain of tasks depending on one another, and a second branch off it
- **When** each task is counted against what waits on it
- **Then** the count reaches through the chain, not just to its direct dependents

#### the queue runs right to left, closest to done first

- **Given** one task in every stage of the pipeline, some already started
- **When** the queue is built, with one of them ready to be resumed
- **Then** work outranks planning, planning outranks design, and a resume leads

#### within a rank the task blocking the most goes first

- **Given** two tasks at the same rank, one of them holding up a chain
- **When** the queue is built
- **Then** the one unblocking the most work is dispatched first

#### two tasks blocking the same amount are ordered by their id

- **Given** two tasks at the same rank, neither blocking anything
- **When** the queue is built
- **Then** the older task goes first, so the order is never arbitrary

#### a task parked on the manager is never dispatched

- **Given** two tasks held, waiting on a person
- **When** the queue is built
- **Then** neither is a candidate, because no agent can move them

#### a task something else is already holding is never dispatched

- **Given** two tasks, each claimed by something still running
- **When** the queue is built
- **Then** neither is a candidate, because two agents may not hold one task

#### a resume goes back to a free slot of the model that holds the session

- **Given** a task to resume, whose session belongs to one of two free agents
- **When** a slot is picked for it
- **Then** the other slot of the same model takes it, so the session is kept

#### a task with no session of its own goes to the fastest agent measured

- **Given** a fresh task and two free agents, one measured faster than the other
- **When** a slot is picked for it
- **Then** the faster agent takes it

#### an agent nobody has measured yet is tried before a measured one

- **Given** a fresh task, one measured agent and one that has never run
- **When** a slot is picked for it
- **Then** the unmeasured agent takes it, so the pool learns what it can do

#### agents measured the same keep the order the pool file gave them

- **Given** a fresh task and two agents measured at the same rate
- **When** a slot is picked for it
- **Then** the first one in the pool takes it, so the choice is not arbitrary

#### keeping the session outweighs going to a much faster agent

- **Given** a task to resume whose own model is measured far slower than the other
- **When** a slot is picked for it
- **Then** the session's own model still takes it, because a resume needs it

#### a task whose own agent is busy falls back to the fastest free one

- **Given** a started task whose model has no free slot at all
- **When** a slot is picked for it
- **Then** the fastest agent that is free takes it rather than nothing running

#### only the head of the queue gives up its session for any free slot

- **Given** a task to resume, and no free slot of the model holding its session
- **When** a slot is picked for it, once at the head of the queue and once below
- **Then** only the head takes the wrong model, so lower tasks keep waiting

#### one slot is never handed to two tasks in the same plan

- **Given** two queued tasks and a single free slot
- **When** the dispatch is planned
- **Then** only the task at the head of the queue is dispatched

#### a candidate carries the role the state it sits in is run by

- **Given** one task in each of the reviewed and reviewing states
- **When** the queue is built
- **Then** each candidate names the role its stage is run by

#### a slot restricted away from the candidate's role is passed over

- **Given** a task needing a worker, and a free slot restricted to reviewing
- **When** a slot is picked for it
- **Then** the unrestricted slot takes it

#### nothing is dispatched when no free slot may take the role

- **Given** a task needing a worker, and only reviewer slots free
- **When** the dispatch is planned
- **Then** the task waits rather than going to an agent restricted from it

#### nothing is dispatched when every slot is already busy

- **Given** a queued task and no free slot at all
- **When** the dispatch is planned
- **Then** nothing is dispatched, and the task keeps its place in the queue

#### a review with a session to pick back up outranks fresh work

- **Given** fresh work and a review whose session can be resumed
- **When** the queue is built
- **Then** the resume leads the queue, as any resume does

### Feature: what is waiting on the manager

#### what is closest to closed comes first

- **Given** a new task, two held ones and a task waiting on the manager
- **When** the inbox is built
- **Then** the manager sees what is nearest to done at the top of it

#### only what is actually waiting on a person is in the inbox

- **Given** tasks that are queued, running, reviewable by an agent, or blocked
- **When** the inbox is built
- **Then** none of them appears, because the pipeline can move them all itself

#### within a rank the task blocking the most goes first

- **Given** two tasks waiting on the manager, one of them holding up a third
- **When** the inbox is built
- **Then** the one unblocking other work is what the manager is shown first

#### a held row carries the reason the manager has to answer

- **Given** a task held on something only a person can resolve
- **When** the inbox is built
- **Then** the row says what it is waiting on and since when

#### a row names the branch to look at, not the worktree it was built in

- **Given** a task waiting on the manager, with a workspace behind it
- **When** the inbox is built
- **Then** the manager is pointed at the branch, which is what outlives the run

## orchestrator/policy/settle.test.ts

### Feature: settling an agent that finished its turn

#### a designer that appended its section hands the turn in

- **Given** a designer settled after calling submit
- **Given** it appended a design section and left the worktree alone
- **When** the server decides what to do with the settle
- **Then** the task is submitted to the next stage

#### a worker hands in its assignment as the new task body

- **Given** a worker settled after calling submit
- **Given** it appended notes and committed its work
- **When** the server decides what to do with the settle
- **Then** the assignment becomes the task body

#### a reviewer that found nothing accepts the work

- **Given** a work reviewer settled after calling submit with no findings
- **When** the server decides what to do with the settle
- **Then** the task moves on without a body change

#### a reviewer that found defects sends the work back

- **Given** a work reviewer settled after calling submit with findings
- **When** the server decides what to do with the settle
- **Then** the findings become feedback

#### a designer that appended nothing is asked for a design

- **Given** a designer settled after calling submit
- **Given** the assignment came back exactly as it was dispatched
- **When** the server decides what to do with the settle
- **Then** the missing-design issue is raised against it

#### a planner that appended nothing is asked for todos

- **Given** a planner settled after calling submit
- **Given** the assignment came back exactly as it was dispatched
- **When** the server decides what to do with the settle
- **Then** the missing-todos issue is raised against it

#### a worker that appended nothing is asked for its notes

- **Given** a worker settled after calling submit
- **Given** the assignment came back exactly as it was dispatched
- **When** the server decides what to do with the settle
- **Then** the missing-notes issue is raised against it

#### a worker that rewrote the assignment above its section has it restored

- **Given** a worker settled after calling submit
- **Given** it changed the assignment above the notes it was allowed to append
- **When** the server decides what to do with the settle
- **Then** everything above its heading is restored
- **Then** the modified-assignment issue is raised against it

#### a reviewer that touched the assignment at all has it restored

- **Given** a plan reviewer settled after calling submit
- **Given** it appended to an assignment it may only read
- **When** the server decides what to do with the settle
- **Then** the whole file is restored, with no section kept
- **Then** the modified-assignment issue is raised against it

#### a worker that committed nothing is told its work is uncommitted

- **Given** a worker settled after calling submit with notes appended
- **Given** its branch carries no commit of its own
- **When** the server decides what to do with the settle
- **Then** the uncommitted issue is raised against it

#### a designer that wrote to the worktree is told to undo it

- **Given** a designer settled after appending its design
- **Given** it left two uncommitted files in the worktree
- **When** the server decides what to do with the settle
- **Then** the modified-worktree issue is raised against it

#### an agent that stopped without a result tool call is nudged for one

- **Given** a worker settled having called no result tool
- **When** the server decides what to do with the settle
- **Then** the missing-result issue is raised against it

#### an agent that ran out of context is nudged for a result

- **Given** a worker whose turn ended because it ran out of context
- **When** the server decides what to do with the settle
- **Then** the missing-result issue is raised against it, whatever it called

#### an agent that called blocked is raised as blocked with its message

- **Given** a planner that called blocked instead of submit
- **When** the server decides what to do with the settle
- **Then** the blocked issue carries the agent's own message

#### an agent caught repeating one command is raised as looping

- **Given** a worker whose turn was cut short for repeating a command
- **When** the server decides what to do with the settle
- **Then** the looping issue names the command and the limit it hit

#### a provider error backs off instead of blaming the agent

- **Given** a worker whose turn ended in a provider error
- **When** the server decides what to do with the settle
- **Then** the server backs off rather than raising an issue

#### an aborted turn releases the slot without touching the graph

- **Given** a worker whose turn was aborted
- **When** the server decides what to do with the settle
- **Then** the agent is abandoned and nothing is submitted

#### a process that died before settling is abandoned

- **Given** a worker whose process is no longer alive
- **When** the server decides what to do with the settle
- **Then** the agent is abandoned

#### a loop is decided before the result the agent managed to call

- **Given** a worker that both looped and called submit
- **When** the server decides what to do with the settle
- **Then** the loop is what it hears about
